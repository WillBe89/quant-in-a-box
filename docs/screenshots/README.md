# Screenshots

Real screenshots taken from the running app, showcasing each major capability, are pending a live capture pass (blocked on a temporary tool-level issue in the build environment as of 2026-07-13 — see the main [README](../../README.md#screenshots) for the current placeholder gallery). Once captured, this folder will hold one PNG per capability named descriptively (e.g. `workspace-indicators.png`, `forecast-overlay.png`, `academy-quiz-badge.png`), referenced directly from the README and the GitHub Release notes.
