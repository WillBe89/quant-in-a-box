# Build Log

Running log of autonomous build cycles. Newest entries at the top.

## 2026-07-13 - Published v1.1.0 (phase 13, part 2: release)

**Built:** finished Phase 13 - bumped `package.json` to 1.1.0, tagged and published a real GitHub Release covering all 45 commits since v1.0.0, organized by theme (data & analytics, chart/workspace UX, portfolio management, the Academy, asset universe) rather than a flat commit list. Shipped without real screenshots - the Browser pane's capture tooling stayed stuck for an extended stretch of this session (a genuine tool-level blocker, not an app problem), and Will chose to ship the release now rather than wait; the README's screenshot section already carries this caveat plainly rather than hiding it.

**A real CI infrastructure bug found and fixed along the way:** the first `v1.1.0` tag push built macOS and Linux installers successfully but failed twice in a row on Windows - `better-sqlite3` couldn't find a prebuilt binary matching the runner's exact Node patch version, fell back to compiling from source via `node-gyp`, and that fallback itself failed because `node-gyp@10.1.0` doesn't recognize the Visual Studio version GitHub's `windows-latest` runner image now ships (`unknown version "undefined"`). Confirmed this wasn't anything in this session's own changes - the exact same failure reproduced on a clean rerun with no code touched. Fixed by pinning the CI workflow's Windows runner to `windows-2022` instead of the floating `windows-latest` label, the standard mitigation for this class of upstream node-gyp/runner-image mismatch. Deleted the incomplete first `v1.1.0` tag/draft release and re-tagged cleanly once the fix was confirmed green, rather than leave a Windows-less draft lying around.

**Verified:** all 4 CI jobs (typecheck/test, and Package on ubuntu/windows-2022/macos) green on the final tag build; confirmed the published release carries all three installers (`.exe`, `.dmg`, `.AppImage`) plus their auto-updater manifests, and that the release body rendered the prepared notes correctly before publishing.

## 2026-07-13 - README overhaul (phase 13, part 1: content)

**Built:** the README hadn't been touched since the v1.0.0 era and read as if the app had stopped at Phase 3 - it was missing every feature shipped since (the forecast overlay, glass redesign, the entire Academy quiz/badge/certificate system, the international/ETF asset expansion, the BYOK data layer, proactive real-data accumulation, portfolio risk grading, the asset browser, and more). Rewrote it directly (not via a multi-agent Workflow - written content like this doesn't need that kind of orchestration) to reflect the app's actual current shape: an expanded Features section organized by area, a new honest "Data & API Keys" section (what each of Finnhub/TwelveData/CoinGecko/Anthropic's free tier actually unlocks, with an explicit caveat that stale real data can be worse than the mock defaults), a new "Not Affiliated, Not Redistributing" section naming every third party referenced anywhere in the app, an updated Known Limitations list paired with today's real mitigation for each, and a new honest Roadmap section (real trade execution, full historical backfill, a portable Academy credential, the drag-drop dashboard redesign) explicitly framed as cost-gated future work, not promised free upgrades.

**A confirmed, not assumed, claim:** the README states that a paid-tier API key works automatically with no code changes needed - checked this directly by reading `finnhubAdapter.ts`, `twelveDataAdapter.ts`, and `coinGeckoAdapter.ts` fresh; none of them hard-codes a free-tier assumption, every one just attempts the real request for whatever key is configured and only falls back to mock on an actual failed response.

**Deliberately incomplete for now:** the Screenshots section ships with clearly-labeled placeholders, not real images - the Browser pane's screenshot/zoom capture has been stuck (consistent timeouts, tried across a fresh dev server, multiple tabs, and a different browser automation surface) for an extended stretch of this session, a genuine tool-level blocker rather than an app problem. A `docs/screenshots/` stub explains the gap. Real screenshots, the version bump, and the actual tagged GitHub Release are held for a follow-up pass once capture works again (or Will supplies his own).

## 2026-07-13 - Proactive CoinGecko real crypto history accumulation (phase 8.9)

**Built:** direct follow-up to Phase 8.8 - Will connected a real CoinGecko API key (Demo tier, 10,000 credits/month) and asked to "start pulling that too," since Phase 8.8 only ever called CoinGecko reactively (a per-view fallback when a chart happens to be open). Added a third background accumulator alongside the existing Finnhub daily-quote one (Phase 8.6) and TwelveData bulk backfill (Phase 8.8) - a new `coinGeckoAccumulator.ts` that captures one real day of price data per relevant crypto asset and derives a single daily candle from it. CoinGecko's free tier can't force daily-resolution bars the way TwelveData's single-call backfill can (its OHLC endpoint auto-picks granularity from the requested day range), so this mirrors the Finnhub accumulator's "one real day at a time, going forward" shape instead of TwelveData's "one big historical grab" shape - fetches the finest available granularity (`days=1`, real 30-minute bars) and collapses it into a proper daily open/high/low/close.

**Deliberately conservative despite the generous budget:** scoped to watchlist/portfolio-relevant crypto assets only, not the full ~490-asset generated crypto universe - leaves the vast majority of the monthly credit budget unused as headroom, matching this app's established restraint on Phase 8.6's own accumulator rather than maximizing usage for its own sake. Runs independently of whether a TwelveData key is also configured, gated solely on having a CoinGecko key, directly answering the literal ask rather than treating CoinGecko as only ever TwelveData's fallback.

**Verified:** `npm run typecheck`/`test` (278/278, including 12 new tests covering the poll-log date-boundary logic and the daily-candle aggregation math with a deliberately non-trivial fixture where the high/low come from middle bars, not the first/last)/`build` all clean. Live in the browser (mock mode): confirmed zero network calls and zero console errors when no CoinGecko key is configured; confirmed the new one-line accumulation note renders correctly in English and Spanish. Real end-to-end confirmation (a genuine daily candle landing in SQLite from a live CoinGecko response, and the poll-log correctly advancing across more than one calendar day) needs Will's own key observed over a few days - the same known boundary as every prior BYOK accumulator phase this session.

## 2026-07-13 - Academy quiz content: Thai translation (261/261 questions)

**Built:** the last item in the long-queued translation backlog - the Academy's 261-question quiz bank (17 lessons, prompt + 4 options + explanation each) was English-only since Phase 10, deliberately deferred pending proof that the English content itself was sound. With Phase 18's content audit now behind it, translated the full bank into Thai, prioritized ahead of the other 9 untranslated locales per Will's own stated reason for Thai specifically (his girlfriend's main language).

**How:** fanned into 4 parallel translation passes by lesson cluster (asset types, trend & momentum, risk & portfolio, options & Greeks) so no single pass had to hold the full 261-question context, each instructed to first read the existing `th.json` lesson content and reuse its established Thai financial terminology (Sharpe ratio, volatility, diversification, moving average, strike price, and so on) rather than inventing new phrasing for the same concepts already taught elsewhere in the app. Merged all 4 passes into a new `academy-quiz/th.json` and registered it as a proper i18next namespace resource alongside the existing English one.

**Verified:** mechanically confirmed 261/261 keys present (zero missing, zero extraneous, zero duplicates across the 4 merged passes) and that every question's option-array length exactly matches its English source (no options added, dropped, or reordered). A separate, independent review spot-checked 64 questions (about 25% of the bank, spread across all 17 lessons) for translation accuracy against the English source and cross-referenced each against `quizQuestions.ts`'s real `correctIndex` to confirm the correct answer still reads correctly in Thai post-translation - all 64 verdicts came back accurate with no mistranslations, mis-orderings, or garbled options. `npm run typecheck`/`test` (266/266)/`build` all clean.

**Known limitation, stated plainly:** like this app's other 10 non-English locales, this is AI-generated translation. It should get a native Thai speaker's review pass before being fully trusted for real users, particularly for idiomatic nuance an automated check can't fully certify - the same caveat already logged for the original 10-language translation pass.

## 2026-07-13 - Widen TwelveData BYOK scope + add CoinGecko for crypto (phase 8.8)

**Built:** the deep-research pass earlier this session found every "bulk download it all" historical-data source has a real legal blocker, but this app's existing bring-your-own-key (BYOK) model for Finnhub/TwelveData is clean since a user's own key for their own personal use isn't redistribution. Separately confirmed Finnhub's Candles endpoint is Premium-only for stocks, crypto, forex, and bonds alike - so TwelveData (which does cover all of these on its free tier) now gets preferred for stocks/re/bonds/fx whenever a key is configured, not just bonds/fx as before. Crypto gets its own new path since neither Finnhub nor a bare-ticker TwelveData call served it before: added a new `coinGeckoAdapter.ts` using CoinGecko's public/keyed `/coins/{id}/ohlc` endpoint (honestly documented granularity: full daily resolution only within 30 days, coarser beyond that, exactly what the free tier actually returns), preferring TwelveData first if configured, falling back to CoinGecko (keyed or keyless) otherwise. A new `twelveDataBackfill.ts` does a genuine proactive multi-year backfill (up to ~5,000 daily bars in one call) for watchlist/portfolio symbols when a TwelveData key is configured, budgeted well under its 800-calls/day cap - a real step up from reactive-only fetching, matching Will's own report of very light usage so far (5 of 800 daily tokens).

**Also built:** a 4th CoinGecko row in Customize alongside Finnhub/TwelveData/Anthropic, plus (on all 4 rows, not just the new one) a real "Get a free key" signup link and an honest per-provider benefit popover - explicitly not overselling what a free tier actually unlocks (e.g. Finnhub's popover says plainly that historical candles are paid-tier-only even with a key connected). One general caveat line near the top of the section: outdated or low-resolution real data can make the analysis *less* useful than this app's internally-consistent mock defaults, not more - connecting a key is a genuine upgrade only when the data behind it is actually current and complete.

**A real bug caught by independent verification, fixed before shipping:** BTC/ETH/SOL - the app's own default watchlist coins - live in a separate hand-curated list from the 490 CoinGecko-sourced generated crypto entries, so the mechanical `coingeckoId` merge done ahead of this phase (matching a fetched ticker-to-id mapping against the generated list) never touched them, silently leaving the app's headline crypto assets unable to use the very feature this phase added. Fixed directly by adding their real CoinGecko ids (`bitcoin`/`ethereum`/`solana`) straight onto the curated entries. A second, smaller bug also caught and fixed: the TwelveData row's own label still read "(bonds & FX supplement)" after the routing was widened, contradicting its own benefit popover - updated to "(stocks, bonds, FX & crypto)" across all 11 locales.

**Verified:** `npm run typecheck`/`test` (266/266, including 11 new CoinGecko-adapter tests and 9 new backfill-log tests)/`build` all clean. Live in the browser: confirmed all 4 key rows' real signup hrefs and distinct, honest per-row benefit copy; confirmed the CoinGecko key round-trips through localStorage and reverts on Clear; confirmed the backfill trigger makes zero network calls with no TwelveData key configured; confirmed live, after the two fixes above, that BTC/ETH/SOL now carry real `coingeckoId` values and the TwelveData label reads correctly. Real end-to-end confirmation against live Finnhub/TwelveData/CoinGecko responses needs Will's own configured keys, same boundary as every prior BYOK phase this session.

## 2026-07-12 - In-quiz study reference + collapsible quiz disclaimer (phase 19)

**Built:** Direct follow-up to phase 18's new pre-quiz study screen - Will asked for the same study content to be reachable *during* an active quiz, not just before it starts, so a stumped user has somewhere real to look without losing progress or reaching for outside AI. He also asked for the anti-cheating disclaimer to become collapsible/dismissible like the chart's forecast disclaimer already is, reasoning that the quiz already has a second, smaller persistent footer reminder on every question, so collapsing or dismissing the big block never leaves zero reminder.

**Collapsible disclaimer:** added `quizDisclaimerMode: 'full' | 'compact' | 'hidden'` to `AppStateContext.tsx`, a genuinely separate piece of state from the chart's `forecastDisclaimerMode` (own `qiab:quizDisclaimerMode:v1` localStorage key, independently toggleable - verified live that dismissing one never affects the other). The big first-question disclaimer block now collapses to a real one-line compact version, expands back, or dismisses entirely, reusing the exact same chevron/close icon pattern the chart's forecast disclaimer already uses. The small persistent footer shown on every question was left completely untouched and unconditional - traced directly in the JSX (no `quizDisclaimerMode` guard of any kind) and confirmed live across all three modes and multiple questions that it never disappears.

**In-quiz study reference:** `QuizRunner` gained a `moduleId` prop and a new header button that opens the phase-18 `ModuleStudyScreen` as a layered overlay directly on top of the running quiz, in a new `contentOnly` mode that hides its own Start/Skip actions (those don't make sense mid-quiz). Opening and closing it is purely visual - the quiz's own question index and selected answers are completely untouched underneath, confirmed live by answering 3 questions, opening the overlay, closing it, and finding the exact same question with the exact same answer still selected.

**Verified:** `npm run typecheck`/`build` clean, `npm run test` 246/246 clean (run directly by me after the workflow finished, not just trusted from its own report). Both the implement and an independent verify pass tested live in the browser: mode persistence survives a full page reload and exiting/restarting the quiz; the study overlay shows real, module-matched lesson content (checked against two different modules to rule out a hardcoded one); i18n parity confirmed non-empty and genuinely translated (not English-fallback) across all 11 locales. Zero bugs found by the independent verify pass.

**A flagged (not a bug) environment quirk:** the verify pass noted `OverlayPanel`'s exit animation sometimes doesn't finish inside the headless browser-automation tool used for testing - traced to the tab never reporting itself as visible in that harness (`document.hidden` stuck `true`, animation frames never firing), and confirmed the same symptom already exists on the pre-existing, untouched Academy overlay too - a test-harness artifact, not something this phase introduced. Worth a quick real-window click-through if extra assurance is wanted, but not blocking.

## 2026-07-12 - Academy pre-quiz study screen + 3 real content-gap fixes (phase 18)

**Built:** Will's observation - the Academy "rushes you into questions" with no diagrams or notes shown first, plus a sharp question: does the library actually have enough information to answer correctly, since "we can't tell a user not to use AI, but if we don't provide the content, where else can they look?" Investigated both parts before building anything.

**The audit:** ran a full 4-way audit (one pass per category) cross-checking all 261 quiz questions against that same lesson's own summary/formula/howToUse/watchOutFor text, independently re-deriving every numeric/calculation answer rather than trusting stored explanations. Result: 258/261 (99%) were already fully answerable from the library exactly as designed, with zero miscalculated correct answers found anywhere. Three genuine, narrow gaps: a volatility question asking what "252" means in the annualization formula (never explained - fixed by adding a clarifying clause); two Black-Scholes questions asking what "strike price" and "underlying price" mean (the lesson only ever used them as bare K/S labels - fixed by adding real definitions to the lesson text). All three now verified directly answerable from the updated text.

**The actual UX gap:** confirmed by reading the code - clicking "Start" on a module went straight to question 1, zero intermediate content of any kind. Built a real "study before you begin" screen shown first, reusing the Library's exact existing visual pattern so it feels like the same app: each module's lessons render in full (summary/formula/howToUse/watchOutFor), plus real diagrams where one is a genuine convention rather than invented for its own sake - a small illustrative chart per trend lesson (moving average, Bollinger Bands, RSI, MACD, each showing only that lesson's own indicator) and a hand-drawn SVG call/put payoff diagram for the options lessons. Asset-type and risk lessons stay text-only, since neither has a standard diagram the way a crossover or a payoff does. The Final Exam gets a lighter "quick review" instead - all 17 lessons' one-line summaries, not the full content again, since by the time it's unlockable the user has already seen every lesson's full study screen once. None of this is a forced gate - a "Skip to quiz" link sits at the top for anyone retaking a module they already know, matching this app's established stance of trusting the user's own judgment.

**Verified:** `npm run typecheck`/`test` (246/246)/`build` all clean. Live in the browser myself: confirmed the trend module's study screen renders all 4 lessons with real, distinct charts (16 real canvases); confirmed the options payoff SVG is a genuine, correctly-shaped hockey-stick call line and mirrored put line with the strike marked; confirmed clicking "Start quiz" launches the real sampled quiz unchanged; confirmed the blackscholes lesson now actually defines strike price and underlying price in plain English; unlocked the Final Exam via its real progress schema and confirmed the quick-review screen lists all 17 lessons grouped by category, genuinely lighter than the subject screens.

**A deliberate non-fix:** the module-card buttons still read "Start quiz"/"Retake quiz" even though they now open the study screen first, not the quiz directly. Left as-is rather than touching 11 translated locale strings for what's genuinely optional copy polish (most quiz software shows a rules/intro screen before "Start Quiz" leads anywhere) - noting the judgment call here rather than silently skipping it.

## 2026-07-12 - A distinct trash-can icon for deleting a portfolio

**Built:** Will noticed delete and close used the same X icon on a portfolio's own page. Added a new `IconTrash` (matching the existing hand-drawn stroke-SVG style already used by every other icon in `Icons.tsx`) and swapped it in for both places a portfolio's delete trigger renders an icon - `PortfolioPane.tsx`'s own header and the equivalent row in Customize's portfolio list, which had the identical issue. Close stays the X everywhere; delete is now a trash can everywhere.

**Verified:** `npm run typecheck`/`build` clean. Confirmed live via DOM inspection that the delete and close buttons now render genuinely different SVG paths, not just visually inspected the same markup.

## 2026-07-12 - Fixed the duplicate NVDA/AAPL/MSFT/BTC/ETH/SOL search results

**Built:** every symbol search result list in the app (Topbar, asset browser, the portfolio's own Add Position box, the chart-slot symbol popover) has been quietly showing NVDA, AAPL, MSFT, BTC, ETH, and SOL twice each, throwing a real React duplicate-key console warning everywhere it happened. Root cause, fully diagnosed this cycle (an earlier guess blaming raw duplicate rows inside the generated asset data turned out to be wrong - only one entry per symbol exists there): `mockData.ts` builds the full stock/crypto list as a plain concat of a small hand-picked "default watchlist seed" array with the much larger real, generated NASDAQ-Trader/CoinGecko-sourced universe - and those six tickers happen to be common enough that they were already present in both lists independently, with nothing deduping between them.

Fixed by merging instead of concatenating blindly: each hand-picked entry now absorbs whatever richer metadata (sector/industry/market cap/country/IPO year) its generated counterpart has, and that same symbol is then dropped from the generated list before the two are joined - so nothing renders twice, and the well-known six actually end up with *more* real data than before, not less. Kept the hand-picked entries in their original first position, since something elsewhere in the app relies on "the first stock/crypto in the list" as the default startup chart symbol (NVDA/BTC) - confirmed via `git diff` that this fix keeps that assumption intact.

**Verified:** `npm run typecheck`/`test` (246/246)/`build` all clean. Live in the browser myself: searched AAPL, NVDA, and MSFT in three different places (the Topbar's global search, the asset browser, and the portfolio's own Add Position search) and confirmed exactly one result each, everywhere; confirmed the default startup chart still opens on NVDA.

## 2026-07-12 - Portfolio rename/delete in-page, a holdings-charts dashboard, and a volume toggle (phase 17)

**Built:** three pieces of live feedback in one message. (1) Rename and delete are now right on the portfolio's own page (a click-to-edit name, a two-step "Delete? / Confirm delete?" button) instead of only reachable via Customize — reuses the exact same `renamePortfolio`/`deletePortfolio` functions and the same two-step confirm pattern already established there, and deleting the currently-open portfolio closes the pane cleanly rather than leaving it showing something that no longer exists. (2) A new "Charts" tab alongside Holdings/Dashboard shows a real mini price chart per holding (not just the existing statistical breakdown) with its own price/change/portfolio-weight line — capped at 6 cards with a "show all" expand, mirroring the same show-top-N pattern already used elsewhere in that same dashboard tab rather than an unbounded render. Clicking a card focuses that symbol in the main workspace and closes the portfolio pane. (3) The chart's volume histogram, previously always-on with no way to turn it off, is now a proper toggleable indicator chip like MA20/RSI/etc — defaulting to on for everyone (so nothing changes for anyone who doesn't touch it), following the exact same widening + migration-guard mechanism used when Forecast was added as an indicator.

**Verified:** `npm run typecheck`/`test` (246/246)/`build` all clean. Live in the browser myself: renamed a portfolio and confirmed the new name propagated to the Rail immediately; deleted the currently-open portfolio and confirmed the pane closed cleanly with the position actually gone from storage (confirmed the two-step confirm genuinely requires two clicks — the first only arms a "Confirm delete?" state); added a real AAPL position, opened the Charts tab, and confirmed a real chart card rendered with correct price/change/100% weight and a genuine `lightweight-charts` instance (7 real canvases); confirmed the Volume chip on a fresh chart slot is on by default, sitting correctly alongside MA20's own pre-existing on-by-default state (double-checked via `git diff` that MA20's default wasn't touched by this change, just the new key added alongside it).

**A pre-existing bug rediscovered, not fixed here:** searching "AAPL" (or NVDA/MSFT) in the portfolio's own Add Position box returns two separate entries for the same ticker, throwing a real React duplicate-key warning — confirmed live myself in this same verification pass. Root cause fully diagnosed this cycle (not what an earlier, now-corrected assumption thought): `mockData.ts` builds `ALL_ASSETS.stocks` as a plain concat of a small hand-curated seed list (`CURATED_ASSETS_BY_CLASS`, which hardcodes NVDA/AAPL/MSFT as default-watchlist entries) with the full generated NASDAQ-Trader/screener universe, which independently already contains those same three real tickers — with no dedup between the two. Queued as an immediate follow-up fix.

## 2026-07-12 - Vanguard/BlackRock/major ETF curation + composition analysis (phase 16)

**Built:** Will's own direction - he personally invests in Vanguard ETFs and plans to move his investments toward Hong Kong/Singapore. Researched and curated 61 real, current, well-known ETFs across the range he'd actually use: 15 Vanguard Australia funds (VAS, VGS, VDHG, VAP, etc.), 9 iShares/BlackRock Australia funds, 12 other major AU issuer funds (BetaShares/SPDR), 13 Hong Kong-listed funds (Tracker Fund of Hong Kong, CSOP Hang Seng TECH, etc.), and 12 Singapore-listed funds (SPDR STI ETF, ABF Singapore Bond Index Fund, etc.) - all sourced directly from each issuer's own public factsheet, not a secondhand aggregator. Merged into the asset universe with the same exchange-suffix convention as the international-stocks phase (`.AX`/`.HK`/`.SI`).

The composition data (issuer, category, index tracked, expense ratio, top holdings/sector allocation) lives in a new `etfProfiles.ts` lookup. The existing asset insight panel now checks this first: opening a known ETF's profile shows its real composition breakdown instead of trying (and failing) to fetch a generic company profile for what is a fund, not an operating company - a plain stock's profile view is completely unaffected. The app's existing quant analytics (Sharpe/Sortino/volatility/chart/forecast) already work automatically for any asset with price history, so no new math was needed - just making sure these tickers are properly chart-able and that their composition surfaces alongside the numbers.

**One cleanup after the verify pass:** the verify agent caught a genuinely dead i18n key (`assetInsight.etfIssuer`, translated into all 11 locales but never referenced - the issuer is shown as a subtitle under the fund name instead, matching exactly how the existing stock-profile view already shows a company's industry, so no separate labeled field was needed). Removed it from all 11 locale files myself.

**A separate, pre-existing bug surfaced during my own verification, flagged rather than fixed here:** at least AAPL, NVDA, and MSFT exist as duplicate entries in the asset universe (once from the original curated/generated data, again from an earlier NASDAQ-screener enrichment pass), causing real React "duplicate key" console warnings in the search box, asset browser, and chart symbol popover. Not something this phase touched or introduced - spun off as its own follow-up task rather than folded in here, since deduplicating ~13,400 entries safely deserves its own careful pass.

**Verified:** `npm run typecheck`/`test` (246/246)/`build` all clean. Live in the browser myself: opened VAS.AX's insight panel and confirmed every field (issuer, category, index tracked, "0.07% p.a.", full composition text) matches `etfProfiles.ts` exactly; confirmed an ordinary stock (NVDA) still shows its normal company-profile view unaffected.

## 2026-07-12 - Compliant personal-use import tool for restricted exchange listings (phase 15)

**Built:** Will asked to package the restricted exchanges' (ASX/NZX/SGX/HKEX/JPX/Bursa/SET) listing files into a zip the app could ship — flagged directly before building anything that this doesn't actually avoid the redistribution restriction those exchanges' terms impose: it's the act of us copying/republishing their content to a third party that's barred, not something a recipient having to do more work afterward changes. Built the compliant alternative instead, which gets the same practical value: every one of these exchanges' terms explicitly permits an individual's own personal, non-commercial use — so instead of hosting a copy, Customize now has direct links to each exchange's own real download URL (ASX/HKEX/JPX/SET, the 4 that actually publish a bulk file - NZX and SGX don't, so they get an explanatory note instead) plus a local "Import listings file…" tool that reads a file you've personally downloaded onto your own machine and merges it into your own local database. Nothing is ever fetched, cached, or shipped by the app itself.

**A real bug caught before it shipped:** HKEX's own `.xlsx` file lies about its own size — its internal `<dimension>` tag claims only 8 rows while the sheet actually has 17,773. A naive parser trusting that tag would have silently imported almost nothing. Fixed by recomputing the true range from the actual populated cells instead of trusting the file's own metadata.

**Verified with real data, not just fixtures:** the implementing and verifying passes both fetched the real, live ASX/HKEX/JPX files directly and fed them through the actual parse → dedupe → SQLite pipeline (not just the unit-test fixtures) — confirming ~1,988 real ASX rows round-trip correctly, a re-import is a genuine no-op, the HKEX stale-dimension bug is correctly recovered, and a real JPX row (Kioxia, `285A.T`) parses correctly. SET's real file is gated behind bot-protection that blocks non-browser fetches, so that one could only be confirmed as the correct, currently-linked URL, not fetched end-to-end here. Independently re-verified the highest-stakes property myself: grepped the entire repo and build output for any committed exchange listing file or embedded exchange-file header text — none exists anywhere.

**One gap I fixed after the verify pass:** the new `userAssets` table had zero direct tests in `localDb.test.ts`, unlike every other table (candles/news/profiles) which each get several. The verify agent confirmed the underlying code was correct by writing and running temporary tests, but recommended adding permanent ones — I added 6 real tests (round-trip with/without optional fields, upsert-not-duplicate, no-op on empty input, ascending sort order) directly to `localDb.test.ts` myself.

**Verified:** `npm run typecheck`/`test` (246/246, including the 6 tests I added)/`build` all clean. Live in the browser myself: confirmed all 4 exchange links have the exact real, verified hrefs and open via `target="_blank"` (routes to the OS browser through this app's existing external-link handler); confirmed the NZX/SGX explanatory note renders in place of a non-functional link.

## 2026-07-12 - Real Oceania/Asia/SEA stock listings: ASX, NZX, SGX, HKEX, TSE (phase 14)

**Built:** searching "Xero" turned up nothing, exposing that the entire stock universe (~13,159 tickers) was sourced exclusively from NASDAQ Trader's US-only public symbol directories. Before building anything, checked whether any Asia-Pacific/Oceania exchange publishes a free bulk listing file safe to bundle into a publicly-distributed app the same way NASDAQ Trader's files were used originally — checked ASX, NZX, SGX, HKEX, JPX, Bursa Malaysia, and SET Thailand directly against their real Terms of Use. None qualify: every one restricts reuse of site content (including a plain symbol/name listing) to "personal, non-commercial use" and requires prior written consent to redistribute — exactly what shipping a symbol list in GitHub Release installers would be. Also checked for a clean aggregator (OpenFIGI, Wikidata, MIC registry, GitHub mirrors, commercial data APIs): nothing is both legally clean and a genuine bulk directory at once.

Given that, added ~200 real, well-known companies the same way this app's existing bonds/FX/real-estate lists were built: hand-curated, not scraped. Five parallel research passes each compiled a real, current index constituent list from reputable sources — ASX 50 (Australia, 50 companies, including Xero), the top ~20 NZX companies by cap (New Zealand), the full 30-constituent Straits Times Index (Singapore), the top 50 Hang Seng Index constituents by weight (Hong Kong), and the top 50 Nikkei 225 constituents by weight (Japan) — then spot-checked five uncertain-looking entries against real sources before merging (all confirmed correct, including one surprise: Pilbara Minerals genuinely rebranded to "PLS Group Limited" in December 2025, which looked like an error but wasn't).

Each new ticker gets a Yahoo/Google-Finance-style exchange suffix (`.AX`/`.NZ`/`.SI`/`.HK`/`.T`) — instantly recognizable, and inherently collision-proof against the existing bare US-only symbols. `Asset` gained an optional `exchange` field (`country` already existed from an earlier enrichment pass). Real price/quote data for these new tickers is a separate, harder problem not addressed here — they run on the same deterministic seeded mock-price generator as any other not-yet-enriched generated asset, upgrading automatically to real data if a configured provider happens to support the symbol format.

**A real TypeScript limit hit and fixed along the way:** adding 200 entries with new field diversity (new sector/country string values, plus the new `exchange` field) pushed the already-enormous `GENERATED_STOCK_ASSETS` array literal (13,359 entries in one file) over a hard internal TypeScript ceiling — "Expression produces a union type that is too complex to represent" (TS2590). Neither a type annotation nor a type assertion fixed it (both still require TS to infer the literal's own type first); the actual fix was a `// @ts-expect-error` directly above the declaration, which is the standard, narrowly-scoped resolution for this specific known TS limitation on huge generated-data literals — it doesn't suppress checking anywhere else in the file.

**Verified:** `npm run typecheck`/`test` (216/216)/`build` all clean. Live in the browser myself: searching "Xero" now returns `XRO.AX` (the whole reason for this phase); confirmed Toyota (`7203.T`), Tencent (`0700.HK`), and DBS Group (`D05.SI`) are all findable with correct names/prices; confirmed Fisher & Paykel Healthcare correctly shows up as two distinct entries (`FPH.AX` and `FPH.NZ`) since it's genuinely dual-listed on both exchanges; confirmed selecting a new international ticker's row updates the focused chart slot exactly like any existing asset; confirmed the total asset count is exactly 13,359 (13,159 + 200, no accidental duplicates).

**Deliberately out of scope, flagged for later:** Bursa Malaysia and SET Thailand weren't researched to the same depth this pass (their licensing was checked and is equally restrictive, but no curated list was compiled) — a natural next step if broader SEA coverage is wanted. Real live price data for these 200 new tickers via Finnhub/TwelveData/CoinGecko hasn't been checked.

## 2026-07-12 - Topbar declutter and a searchable/sortable asset browser (phase 12)

**Built:** the Topbar's six class-filter chips (All/Stocks/Crypto/Bonds/FX/Real Estate) now each open a new full asset browser panel pre-filtered to that class, instead of doing nothing else useful beyond being visual filters. The browser handles the full ~13,159-asset universe with plain Prev/Next pagination (100 rows/page, 132 pages on "All") rather than a virtualization dependency — this project's established preference for hand-rolling something this simple over adding a new package. It reuses the existing `searchAssets` ranking logic for its own search box, has sortable Symbol/Name/Price/%Change column headers (click toggles ascending/descending), and reuses the same class-filter chip styling as in-panel controls. Selecting a row sets that asset as the focused chart slot's symbol via the existing `selectSymbol` mechanism — the same one the Topbar search and Rail watchlist already use — and closes the panel. Separately, the previously-shipped Topbar declutter (removing the duplicate Portfolio/Learn buttons and relocating the "Analytics only" note into Customize) was confirmed still intact, not rebuilt.

**Verified:** `npm run typecheck`, `npm run test` (216/216, including 5 new tests for the pagination-math helper), and `npm run build` all clean. Live in the browser myself: clicked Crypto, Real Estate, and All chips and confirmed the browser opens correctly pre-filtered each time (caught and ruled out a false alarm here — my first pass at this used coordinate-based clicks against an Arabic RTL layout, which landed on the wrong elements entirely, including once flipping the app's language; switching to direct JS-driven clicks on the actual elements confirmed the feature genuinely works); confirmed sorting the Price column both directions reorders rows correctly; confirmed pagination shows "Page 1 of 132" on the full universe and correctly clamps to a single page on the 19-asset Real Estate class; confirmed selecting a row updates the focused chart slot's symbol and the panel closes (via its normal exit animation, which briefly kept it in the DOM and initially looked like a bug until I waited past the transition). Also used this pass to confirm live, directly in the running app, that searching "XRO" (Xero) returns zero results — concrete, first-hand confirmation of the international-stock coverage gap flagged below.

## 2026-07-12 - Two real bugs in the asset browser, caught by independent verification (phase 12 follow-up)

**Built:** the phase 12 workflow's own verify agent (which kept running through a context-compaction boundary and reported back afterward) found two genuine bugs in `AssetBrowserPanel.tsx` that my own live-in-browser check had missed: (1) the Prev/Next pagination buttons captured `safePage` in a stale closure rather than using React's functional-update form, so rapid repeated clicks (an impatient triple-click, or keyboard auto-repeat on a focused button) advanced the page by only 1 net instead of 3 — fixed by switching both handlers to `setPage((p) => p ± 1)`; (2) the panel's search box never resets when reopened via a different Topbar chip, since the panel component itself never unmounts (only its overlay animates in/out) — so searching "AAPL" under Stocks, closing, then opening Crypto would show a confusing "0 assets" screen with "AAPL" still sitting in the box. Fixed with a `useEffect` that clears the query specifically on the closed-to-open transition, without clobbering an in-progress search when a chip *inside* the already-open panel just narrows the class filter.

**Why this matters for how this project verifies work:** my own live-browser pass on this phase used unreliable coordinate-based clicks against an Arabic RTL layout (misfires I caught and corrected in the moment), which meant I tested the feature's correctness but not these two specific interaction-timing edge cases. The workflow's independent verify agent reproduced both directly (three rapid `.click()` calls in one batch; a search-then-close-then-reopen-different-class sequence) rather than relying on a single click's happy path, which is exactly why this project always runs a separate, adversarial verify pass instead of trusting the implementer's own testing.

**Verified:** re-ran `npm run typecheck`/`test` (216/216)/`build`, all clean. Live in the browser myself: reproduced both bugs before the fix (confirmed the stale search leaked across a chip-triggered reopen, and that firing the Next handler 3 times in one batch only advanced 1 page), then confirmed both are gone after the fix (reopening after a search now shows an empty box and the correct full result count; 3 rapid Next clicks now land on page 4, not page 2).

## 2026-07-12 - News images, category tags, and filter controls (phase 11)

**Built:** market news now carries real category tags (general/forex/crypto/merger) and article thumbnails where the source provides one. A new chip row lets you toggle categories on/off — disabling the last active one is a no-op rather than leaving you with zero categories — and it's hidden entirely when you're viewing a single asset's own news, since a single-symbol feed has no category dimension to filter. One failing category's fetch never blanks the others (each fetched independently, merged and deduped by article id, sorted newest-first). Since the local news table already exists in shipped installs, adding the new `category`/`image` columns needed a real migration (`PRAGMA table_info` + conditional `ALTER TABLE`), not just an edited table-creation statement.

**Verified with real rigor on the highest-risk part:** rather than only testing the migration against a fresh database, the implementing pass hand-built a *pre-existing* news table in the old schema shape, seeded a real row into it, then ran the actual migration code against that populated table — confirming the old row survives untouched and the new columns work afterward, not just that a brand-new install works. `npm run typecheck`/`test` (211/211)/`build` all clean, confirmed independently a second time. Live in the browser myself: expanded the Market News card and confirmed article thumbnails render correctly (the new merger-tagged article — "Chipmaker to acquire AI inference startup in an all-cash deal" — showing right alongside a real image), and confirmed the category chip row is correctly absent when news is scoped to a single symbol, exactly matching how the feature is designed to behave.

**What still needs a real key to fully confirm:** a genuine live Finnhub fetch where one category's request actually fails over the network and the others still render (verified via a mocked-fetch test instead, which proves the logic but not the real network behavior) — same honest boundary as every phase in this project that depends on real API behavior no key currently exists here to exercise.

## 2026-07-12 - Collapsible/dismissible forecast disclaimer, plus a critical fix to a regression it exposed (phase 8.7)

**Built:** the "Not a prediction" forecast disclaimer got naggy after repeated exposure, so it can now be collapsed to a compact one-line notice or dismissed entirely — a deliberate, one-off exception to this app's usual "always-rendered, never dismissible" disclaimer rule (AI Insights and the Academy's anti-cheating notice both stay fully permanent, untouched by this change). The choice is global (not per-chart) and reversible: a small control in Customize brings it back to full at any time, so hiding it is a genuine deliberate choice, not a dead end.

**A real, serious regression was caught during this phase's own live verification: the Forecast toggle itself was completely broken.** Clicking the Forecast chip on the chart you were actually looking at would turn it on and then silently revert to off within about 150ms, with no visible error — reproduced consistently through every input method tried (real click, synthetic click, direct state dispatch). This traced back to Phase 8.5's auto-forecast-on-scroll feature, shipped earlier today: turning Forecast on triggers the chart to re-fit its view (to show the new projection lines), which extends the visible range into the same "future space" that Phase 8.5's auto-enable logic watches for — and the guard meant to stop it from re-triggering read a `ref` that mirrors chart-slot state through a `useEffect`, which hadn't caught up yet by the time the chart's own effects fired. So the guard saw a stale "still off" value, didn't bail, and toggled Forecast straight back off — undoing the user's own click, on every single attempt.

**Fixed** by writing that mirror ref directly during render instead of inside a `useEffect` — refs can be safely assigned during render (unlike state), which keeps it permanently a step ahead of any effect-timing race rather than one render behind. Verified directly: clicked Forecast on for a visible chart, confirmed via `localStorage` it stayed `true` (not just visually, checked again after a 2-second wait to rule out a delayed revert), then clicked it off and confirmed that direction still works too.

**Verified:** `npm run typecheck`/`test` (200/200)/`build` all clean. Live in the browser myself: reproduced the exact broken state first (clicked Forecast, watched it silently revert), applied the fix, then confirmed clicking Forecast on the visible chart now works and stays on, clicking it back off still works, and the new collapse/compact/dismiss states all render and persist correctly across a reload, with the Customize recovery control restoring full mode from either alternate state. Independent verification additionally confirmed the disclaimer-mode logic itself was correct throughout — the bug was entirely in Phase 8.5's already-shipped code, not in this phase's own changes, which is why it surfaced only once this phase tried to actually exercise the feature end-to-end live rather than by seeding state directly.

## 2026-07-12 - Real Academy quiz content, all 17 lessons (phase 10)

**Built:** replaced Phase 9's 5-question-per-lesson placeholder bank with a full content bank across all 17 lessons — 261 real questions total, each testing something the lesson's own summary/formula/how-to-use/watch-out-for copy actually teaches, with one correct answer, three plausible (not silly) distractors, a difficulty rating, and an explanation shown after answering. Options & Derivatives (the smallest category, just 2 lessons) deliberately got the largest per-lesson pools (18 each, 36 total) specifically to fix the content-variety gap flagged during Phase 9's verification, where its old pool exactly equaled its module quiz's sample size, so every attempt was the same 10 questions reshuffled.

**Verified with real rigor, since this is content real users will be quizzed on and trust:** every quantitative question (Sharpe, Sortino, VaR, max drawdown, beta, Bollinger, RSI, MACD, Black-Scholes Greeks, bond yield) was hand-recomputed against the actual formulas in `lib/quant.ts`, not just checked against the lesson's prose — including confirming this app's specific sign convention (max drawdown and VaR are both expressed as negative numbers here, not a generic textbook convention) is followed consistently across every question that touches them. The Options variety fix was verified two ways: running the real sampling algorithm with different seeds showed only ~28% average question overlap between two attempts, and a live double-take of that exact quiz in the browser showed only 2 of 10 questions repeated. A standalone structural check confirmed every question's lesson reference resolves, every option count and correct-answer index is in range, no lesson has fewer than 10 questions, and no display string is orphaned in either direction between the question metadata and its i18n text.

**Verified:** `npm run typecheck` and `npm run build` clean (confirmed independently a second time); the implementing pass's own report showed 200/200 tests and a full live click-through of every module including the Final Exam's 35-question cross-category sampling. I didn't re-run `npm test` or a live browser check directly myself for this specific confirmation pass since Phase 8.7 was actively using both the native module and the shared browser session at the time; GitHub Actions' isolated CI run is the authoritative confirmation instead. Zero factual or structural errors found — this is genuinely rigorous content, not a rubber-stamped placeholder replacement.

**Still English-only by design** — translating this content into the other 10 locales (Thai first, prioritized) is queued as a separate follow-up pass, not part of this one.

## 2026-07-12 - Auto-enable the forecast overlay when scrolling into the future (phase 8.5)

**Built:** while asking how the history-download tool works, Will's follow-up: "if I push forward into the future it should automatically set the forecast on." This is a direct sibling to Phase 8's "scroll toward the oldest edge reveals stored history" wiring — same underlying chart-scroll detection, opposite edge. Scroll or zoom a chart forward past the last real candle into empty future space, and the Forecast overlay now switches on by itself (using whichever method was already selected), if it wasn't already on. It's one-directional by design: scrolling back to a fully historical view never auto-disables it — you can always turn it off manually via the Forecast chip, matching exactly what was asked for (auto-enable, not auto-toggle).

**The one subtlety that had to be right**: this needed to fire once on a genuine crossing into future space, not repeatedly on every scroll tick while already there (which would fight with a manual "no thanks" click), and it needed to correctly re-arm if you scroll back to historical and then forward again later. Both the implementing and verifying passes traced this by hand against the actual guard logic and confirmed it holds in both directions.

**Verified:** `npm run typecheck`/`test` (200/200)/`build` all clean, confirmed independently a second time. Live in the browser myself: started with Forecast off on NVDA, scrolled the chart out past the last loaded candle, and watched the Forecast chip light up automatically along with the disclaimer and projection band, with zero console errors. Independent verification additionally confirmed the pre-existing Phase 8 history-reveal behavior on the opposite edge is completely unaffected (shares the same underlying callback but the two checks don't interfere), and hand-traced the edge-triggered guard logic to confirm it fires exactly once per crossing rather than repeatedly. Zero bugs found.

## 2026-07-12 - Daily real-price accumulation via Finnhub's free /quote endpoint (phase 8.6)

**Built:** since Finnhub's `/stock/candle` (historical bars) is confirmed paid-tier-only, this adds the low-risk, immediately-useful piece: a small background accumulator that captures one real daily candle per watchlist/portfolio stock or real-estate symbol via Finnhub's free `/quote` endpoint, once a day. A persisted poll-log keyed by plain date strings (not `Date` objects) prevents re-fetching a symbol already captured that day, staggered roughly a second apart between calls to stay well within the free tier's rate limit even for a large watchlist. Skips entirely, with zero network calls, whenever no Finnhub key is configured. Stores each captured point via the existing `storeCandles` call — no new IPC, no new schema. Volume is stored as 0 (not available from a quote snapshot); confirmed this is safe since this app's risk/return math only ever touches candle close prices, never volume.

**A real, if minor, issue independent verification caught in the code's own comments:** the rationale explaining why crypto/bonds/FX are excluded from this accumulator claimed it was because Finnhub's routing exempts them — that's not accurate. Reading `dataService.ts`'s actual routing logic shows crypto routes through the same blocked Finnhub path as stocks whenever a Finnhub key is configured, and bonds/FX only escape to TwelveData when a TwelveData key is *also* separately configured. The real reason those three classes are out of scope here is that their symbol formats (bare crypto tickers, yield-index values, forex pairs) wouldn't resolve as valid stock quotes via `/quote` regardless of routing — a different, still-correct reason than what the comment said. Fixed both explanatory comments to state the real reason, so a future reader doesn't trust an inaccurate routing claim to make a different call later. The actual scoping (`['stocks', 're']`) was correct all along; only the stated rationale was wrong.

**Verified:** `npm run typecheck` and `npm run build` clean (confirmed independently a second time after the comment fix); the implementing pass's own report showed 200/200 tests passing including 8 new tests for the poll-log date-boundary logic, the quote-to-candle mapping, and per-symbol failure isolation (one bad symbol doesn't abort the rest of the batch, and isn't marked as polled so it retries next pass), plus a live check confirming zero network calls fire when no key is configured. `npm test` wasn't re-run directly by me for this specific fix since three other workflows were actively using the native module concurrently at the time; GitHub Actions' isolated CI run (which runs the full suite fresh) is the authoritative confirmation instead.

**Important limitation, flagged rather than glossed over:** no real Finnhub key exists in this environment, so the actual live `/quote` call and a real quote landing in SQLite were never exercised end-to-end — same boundary as Phase 8's own download tool. The code is built to fail loudly per-symbol (not silently) if a real key's `/quote` calls ever behave unexpectedly.

## 2026-07-12 - Enriched the stock universe with a real NASDAQ screener export

**Built:** Will provided a real NASDAQ screener CSV export (7,124 tickers with real last-sale price, % change, market cap, sector, industry, country, and IPO year). Merged it into `assetUniverse.ts`'s generated stock list by symbol: 5,618 existing symbols got their synthetic price/changePct overwritten with real values plus the new metadata fields (sector/industry/marketCap/country/ipoYear, all newly added to the `Asset` type as optional fields), and 1,506 symbols present in the export but missing from the existing list were added outright — bringing the stock universe from 11,094 to 12,600 entries. Everything not covered by this export (the other ~10,900 non-CSV-matched stocks, plus crypto/bonds/FX/real-estate entirely) is untouched, still running on the original synthetic seed data exactly as before.

**Why this matters beyond just "more accurate prices":** the new sector/industry/market-cap/country/IPO-year fields are real data this app never had before, in any form — a solid foundation for Phase 12's planned sortable/filterable asset browser table, and for richer offline company-profile lookups. Wired the second use immediately: `generateCompanyProfile`'s mock fallback (used whenever no Finnhub key is configured, or for a symbol the live API doesn't have) now prefers this real data per-field when available, falling back to the existing synthetic generation only for whatever the export didn't cover — e.g. AAL's mock profile now shows its real ~$11.2B market cap and "Air Freight/Delivery Services" industry instead of a synthetic placeholder, and the derived share count (market cap ÷ price ≈ 661M shares) lines up closely with AAL's real-world share count as a sanity check.

**Important framing, consistent with how this app already treats all default data:** these are frozen values from the moment the export was taken, not live-updating — same category as the synthetic seed values they replaced, just meaningfully more realistic as a starting point. A live Finnhub/TwelveData key still takes priority for whatever you actually watch or hold, unchanged.

**Verified:** `npm run typecheck` and `npm run build` both clean; hand-verified the merge script's output against the source CSV for several symbols (spot-checked AAL, AACI, AACP) and hand-verified `generateCompanyProfile`'s new real-vs-synthetic branching logic and unit conversion (the export's market cap is in raw dollars, the profile shape expects millions) line by line. `npm test` and a live-browser check are deferred until the three Academy/forecast/data-accumulator workflows already running concurrently in this same working tree finish, since running the test suite's native-module rebuild step right now risks colliding with their own in-progress native-module state — this change touches none of the same files those workflows are editing, so it's safe to land as its own commit in the meantime.

## 2026-07-12 - Academy home page: making the Library/Academy split actually discoverable (phase 9.5)

**Built:** direct feedback right after Phase 9 shipped: the Library/Modules split was just a small segmented toggle buried in the Teaching Zone's header, easy to miss, with no explanation of what either side was for or why there were two. Opening the Teaching Zone now always lands on a proper Home screen first: a short explanation of the two-part purpose, then two large cards side by side. Library card: "learn what each indicator and metric means and how it's actually used." Academy card: "test yourself with graded quizzes and earn badges," with a live one-line progress teaser pulled from real state ("Start earning badges" on a fresh profile, "2 of 5 badges earned" once you've made progress). The quiz/badge section is renamed from "Modules" to "Academy" throughout, matching how it was actually described in the request. A 3-way header toggle (Home / Library / Academy) still lets you jump directly between all three at any time.

**What stayed unchanged, deliberately:** every existing "Learn about this" info-icon link (from Portfolio's risk stats, the Options card, and the chart's indicator rows) still opens the panel straight into Library on the exact lesson it points to, completely bypassing the new Home screen — that's a "show me this specific thing" flow, not a "let me explore" one, and it would be actively worse if every deep-link detoured through an explanatory page first.

**Verified:** `npm run typecheck`/`test` (192/192)/`build` all clean, confirmed independently a second time. Live in the browser: clearing all progress and opening the Teaching Zone from the sidebar landed on the new Home screen with both cards showing real copy (not raw translation keys) and the correct zero-state teaser; clicking the Sharpe Ratio info-icon from the sidebar's Portfolio Risk card opened the panel directly on that lesson in Library, confirming the deep-link path still bypasses Home exactly as intended. Independent verification additionally confirmed the "Modules" to "Academy" rename was applied consistently everywhere (no stale references to the old key left anywhere in the codebase) and that the renamed label genuinely reads as "Academy" (not a lazy no-op still saying "Modules") in all 11 languages, spot-checking Spanish and Arabic (including correct RTL layout) directly.

## 2026-07-12 - Academy foundation: quiz engine, 5 badges, and downloadable PDF certificates (phase 9)

**Built:** the Trading Academy gets a real graded layer on top of its existing lesson library. A new Library/Modules toggle sits in the Academy panel's header; Modules mode shows 5 badges (Asset Classes, Trend & Momentum, Risk & Portfolio, Options & Derivatives, and a gated Final Exam), each backed by a real quiz sampled from a per-lesson question bank rather than a static full-bank test every time, an 80% pass mark, a one-hour retry cooldown, and progress that persists locally and never regresses (a best score only ever goes up, a badge once earned is never revoked). The Final Exam only unlocks once all 4 subject modules have passed. An always-visible anti-AI-cheating disclaimer (both a full block before question one and a persistent footer on every question) makes clear the badge is worthless as a shortcut, not a checkbox to tick past once.

**The two things Will asked for directly this session, on top of the already-planned foundation:** the badge count is locked at exactly 5 (4 subject modules + Final Exam) with simple in-app SVG glyphs for each, so real PNG artwork can now be commissioned against a known, final count. And once a badge is earned, both its results screen and its resting slot on the badge shelf offer a "Download certificate" action: type the name you want printed (remembered for next time, editable per-download), and the app generates a real PDF via Electron's own built-in `webContents.printToPDF` (zero new dependency, reusing the same save-dialog pattern already proven for the Excel export) with that name, the badge earned, and the date. Researched (and written into the project plan, not built yet) how a badge could one day become genuinely LinkedIn-portable: the old parameterized "Add to Profile" autofill link was deprecated by LinkedIn itself around 2024, so real portability now needs a small public, independently-checkable credential page per badge (Open Badges standard, self-hostable on a free static site) — a real, scoped, separate future project, not something this desktop app can complete alone.

**Placeholder content, by design:** this pass ships 5 real, correct, hand-written questions per lesson (85 total across all 17 lessons) rather than the eventual full 10-20-per-lesson bank — enough for every module to sample from without ever crashing or trivially repeating, though the smallest category (Options, only 2 lessons) currently has exactly as many questions as its target sample size, so every attempt draws its whole 10-question pool just reshuffled. A full content-authoring pass is a separate, later task.

**A real bug caught and fixed during the build's own live verification, before independent verification even started:** the results screen's action buttons were gated on the wrong flag, so dismissing the badge-unlock celebration animation left the screen stuck showing only a "Continue" button forever instead of switching to "Back to modules"/"Retake." Fixed and re-verified live via hot reload. A copy bug was also caught and fixed across all 11 locales: the "you passed" message read oddly when someone scored a perfect 100%, since it phrased 100% as if it were the pass mark itself rather than the score achieved.

**One small hygiene fix I made myself after the independent verification pass:** four code comments referenced "the Phase 9 report" or "the Phase-9 plan" as if those were documents living in this repository — they aren't (they're just this session's own internal task descriptions). Reworded all four to be self-contained so a future reader isn't left looking for a document that doesn't exist. Not a functional bug, just something worth tidying before it shipped.

**Verified:** `npm run typecheck`/`test` (192/192)/`build` all clean, confirmed independently a second time after my own comment cleanup. Live in the browser (mock mode, fresh profile): Rail → Teaching Zone still lands on Library unchanged; an info-icon click from inside Portfolio still opens Library directly on that lesson, stacked correctly above Portfolio; Modules mode shows all 5 badges locked with distinct, on-brand glyphs; started a real quiz and confirmed both disclaimer placements render together on question one. Independent verification additionally completed full real quiz attempts end-to-end (a 10/10 pass, cooldown correctly blocking an immediate retry and surviving a reload, and a deliberately-failed 35-question Final Exam once all 4 subject modules were simulated as passed), and hand-verified the pass-boundary and cooldown-boundary math down to the exact millisecond/percentage. I separately confirmed the earned-badge state and the certificate flow myself by setting a passed profile directly: all 5 badges render in full color with a "Download certificate" link each, the name prompt appears and remembers a previously-typed name, and clicking Generate in this plain-browser context (no Electron shell available here) surfaces the intended graceful "Couldn't generate the certificate" message rather than crashing silently.

**Important limitation, flagged rather than glossed over:** `webContents.printToPDF` only exists in Electron's main process, and none of the live-verification tooling available in this session can open the actual Electron shell (only a plain browser tab against the Vite dev server, where the app's IPC bridge is undefined by design). So the certificate's real visual output — border, background, orientation, the typed name and badge glyph actually landing correctly on the page — has only been verified by code inspection and unit-tested pure helpers, never by looking at a real generated PDF. **This needs Will to run the packaged or dev Electron app himself, click "Download certificate," and open the resulting file to confirm it looks right.**

## 2026-07-12 - Explicit historical-data download tool, Finnhub-only (phase 8 of the reprioritized batch)

**Built:** Will's original ask ("when you scroll out it should fill in the historical data") was reframed after flagging API-quota risk; his actual answer was to make it an explicit, user-initiated download, Finnhub-only (TwelveData excluded), gated behind a visible call-count estimate. A new "Download historical data" section in Customize lets you pick a stock (crypto/bonds/FX/real estate aren't supported by Finnhub's candle endpoint, so they're filtered out of the picker entirely), choose 1/2/5/10/20 years, and see the exact call-cost estimate baked directly into the download button's own label before ever clicking it. Downloaded daily candles always store under the same bucket the app already uses for its native daily view, so a bulk download and the app's regular per-view fetching can never mix incompatible resolutions under one key. On the chart side, scrolling back toward the oldest loaded bar checks the local SQLite cache for anything already downloaded and reveals it instantly, with zero new network calls, whether that data came from this new tool or just accumulated from ordinary browsing.

**The one rule this feature had to get right, unlike the app's existing per-view candle fetching**: never silently fall back to mock data on failure. A real download failure now surfaces one of three distinct messages (no data / a permissions-style block / rate-limited) rather than a generic error or a silent mock substitution — because this is a deliberate action the user is paying API calls for, not a passive background convenience.

**Important limitation, flagged rather than papered over:** there is no Finnhub API key configured anywhere in this environment (no `.env` file exists), so the actual behavior of Finnhub's `/stock/candle` endpoint on a free-tier key remains genuinely unverified — including the open question, found during earlier research, of whether free-tier access to that endpoint even works at all or returns a 403. Everything that touches a real key was verified only through mocked HTTP responses in unit tests, not against Finnhub's real servers. The code is built to fail loudly and specifically the first time it runs against a real key, rather than assume success.

**Verified:** `npm run typecheck`/`test` (121/121)/`build` all clean, confirmed independently a second time after the workflow closed out. Live in the browser (no key configured, the true current state of this environment): the new Customize section renders genuinely disabled (`opacity: 0.5`, `pointer-events: none`, every input/button truly `disabled`) with a clear message explaining a Finnhub key is needed, while the years-preset picker stays visible so the shape of the feature is still discoverable; the stocks-only asset picker and per-download call estimate were confirmed correct by code inspection and unit test. Scrolled a real chart back past its oldest loaded bar and confirmed, via the network tab, zero new requests fired — the "reveal already-downloaded history" path only ever reads local SQLite. Independent verification additionally hand-traced the full call chain (chart component to IPC to SQLite) to confirm no code path in this feature can reach a live Finnhub fetch by accident, and confirmed all 11 locales have complete, genuinely translated strings including language-appropriate plural forms for the estimate text (e.g. Arabic's five-way plural set, Russian's three-way set). Zero bugs found.

## 2026-07-12 - Forward-forecasting chart overlay, 3 selectable methods (phase 7 of the reprioritized batch)

**Built:** Will asked directly for forward-forecasting on the charts; asked which method, his answer was "all 3 as different options" — so all three ship as a user-selectable method, not one fixed choice. A new `lib/forecast.ts` module works entirely in bar-index units (not calendar time), since this app's mock candle spacing varies wildly by timeframe (1D = 30-minute bars, 5Y = monthly bars) — projecting "k bars ahead" off per-bar log-returns sidesteps having to guess an annualization convention per timeframe, and `PriceChart.tsx` converts the projection back into real future timestamps by extrapolating the last observed bar-to-bar time delta. Three methods, all returning the same `{central, upper, lower}` band shape: **Drift** (closed-form GBM, anchored off the asset's own last close), **Trend** (OLS regression of bar-index vs. close, extrapolated forward), **Monte Carlo** (~500 simulated GBM paths, percentile-banded). A new `forecast` indicator chip shows a 3-way method picker and a permanent "Not a prediction" disclaimer the instant it's toggled on, matching the same always-rendered disclaimer pattern already used for the portfolio's AI insights.

**A worked example in my own task brief was wrong, caught transparently by both agents:** I'd written a test expectation assuming the forecast anchors off the asset's *first* close; the actual design spec (which I'd also written) explicitly said to anchor off the *last* close, which is the only choice that's mathematically correct for a forward-looking projection. The implementation followed the correct spec, not my flawed inline example, and independent verification hand-derived the drift/regression closed-form math from scratch to confirm the last-close anchor is right.

**Verified:** `npm run typecheck`/`test` (104/104)/`build` all clean, confirmed independently a second time after the workflow closed out. Live in the browser: toggled Forecast on NVDA, the method picker and disclaimer appeared instantly; cycled Drift (widening exponential cone) → Trend (straight dashed line, parallel constant-width band) → Monte Carlo (curved fanning band) and confirmed all three are visually distinct; switched timeframes with forecast on and confirmed the projection continues smoothly and proportionally from the last real bar regardless of bar spacing; toggled forecast off and confirmed the price scale immediately re-fit to just the candle range (proof the three series were actually cleared, not just hidden). Independent verification additionally confirmed the Monte Carlo percentile ordering (`lower <= central <= upper`) holds structurally by construction, not by test luck, and that all 11 locales have genuine, non-copy-pasted translations for the new strings. Zero bugs found.

## 2026-07-11 - Decluttered the Topbar (removed duplicate buttons, relocated the scope disclaimer)

**Built:** part of a new batch of overnight asks. Will flagged directly: "The top buttons are an unnecessary double up of the bottom left buttons, and the note about 'analytics only' can be kept in the settings or somewhere else, it doesn't need to be a constant reminder." Confirmed by direct read: `Topbar.tsx`'s Portfolio button and Learn/Academy button were byte-for-byte functional duplicates of `Rail.tsx`'s own Portfolio and Teaching Zone buttons at the bottom of the left rail — same actions, same badge, reachable two ways for no reason. Removed both from the Topbar, leaving only the language selector and theme toggle (neither duplicated elsewhere). The permanent "Analytics only · no trade execution" badge is now a small one-line note at the top of the Customize panel instead of an always-visible piece of chrome — still discoverable, no longer a constant reminder. Cleaned up the two now-fully-dead i18n keys (`topbar.portfolioBtn`/`topbar.learnBtn`, confirmed unused anywhere else via grep) and the now-dead `.scope-badge` CSS rules, and relocated the disclaimer's existing translated text into a new `customize.analyticsOnlyNote` key across all 11 locales rather than re-translating from scratch.

**Verified:** `npm run typecheck`/`test` (94/94)/`build` all clean. Live in the browser: Topbar shows only search, class filters, language, and theme toggle; Rail's Portfolio/Teaching Zone buttons still work exactly as before; the Customize panel now opens with the relocated disclaimer visible as its first line.

## 2026-07-11 - Glass/fluid visual redesign + chart depth (phase 6 of the reprioritized batch)

**Built:** the app's biggest visual pass yet. Will asked for "glass floating on the background - with the background as a soft gradient... glass/fluid feel throughout the app + make the depth of the graph look better," then, when asked how far to take it, said: "create a settings option for users to select any of the 3/tick on off things they want as glass" — so this shipped as a granular, user-controlled system, not one fixed look.

- **Three independent toggles** in a new "Glass appearance" Customize section, persisted (`qiab:glassTiers:v1`, following the same load/save pattern as dock layout and watchlist — notably *not* copying `theme`'s own behavior, since `theme` turned out not to be persisted at all today, a small pre-existing gap noticed while building this): **Panels & cards** (every modal, all 3 dock cards, every popover — on by default, lowest legibility risk), **App chrome** (Topbar/Rail/Dock/Ticker), **Chart background** (the price chart's own backdrop).
- A soft, subtly animated radial-gradient background on `body`, always on regardless of any toggle, respecting `prefers-reduced-motion`.
- Chart depth: candlesticks now render with a visible per-candle border (previously `borderVisible: false`), plus an always-on soft shadow/glow under the chart panel — scoped to the candlestick style only, bars/line/area/baseline untouched.
- Handled two compounding-risk cases explicitly rather than leaving them to chance: the ticker's scroll-edge fade masks (previously hardcoded to the old opaque color) get their own glass-aware recolor so they don't show a wrong-colored strip once the ticker itself goes translucent; and dock cards sitting on glass chrome (two stacked blurred layers over the dense Options/Greeks numbers) get a dedicated higher-opacity variant specifically for that combination, so the numbers stay legible rather than washing out.
- Every new rule is additive (a new `:root[data-glass-*='true'] .selector` layered after the existing base rule, mirroring the exact `data-theme` mechanism already used for light/dark) — nothing existing was rewritten, so every tier defaults to today's exact look when off.

**Verified, unusually thoroughly given how much surface this touches:** independent verification checked all 8 toggle combinations (each of panels/chrome/chart on or off) in both light and dark theme — confirmed the all-off state is pixel-identical to before this change, confirmed light-theme opacities are genuinely higher than dark's at every token (e.g. dock-card 86%/94%, chrome 66%/80%), confirmed the ticker fade-mask fix and the dock-card compound mitigation both work as intended, confirmed the candlestick border change is scoped to exactly one `switch` branch, confirmed persistence survives a reload while `theme` correctly resets (proving the two systems are genuinely independent, not accidentally sharing logic), and checked the new border colors directly at the canvas pixel level. Zero bugs found in 12 checks. `npm run typecheck`/`build` clean.

## 2026-07-11 - Chart quick-symbol-change + asset insight panel (phase 4 of the reprioritized batch)

**Built:** two related workspace UX features Will asked for directly. First, clicking a chart slot's ticker+name now opens a small popover with the same asset search box the Topbar already has — selecting a result changes only *that* slot's symbol, never the currently-focused one (the critical distinction: the new trigger calls `setSlotSymbol(slotId, asset)` directly, not the shared `selectSymbol`, which always targets whichever slot happens to be focused). Second, a new "company profile" icon next to the existing watchlist star opens an insight popover showing a stocks-class asset's industry, market cap, shares outstanding, exchange, IPO date, country, and website — sourced from Finnhub's `/stock/profile2` (new `profiles` table in the local database, 24h TTL, same cache-then-fetch-then-store pattern as candles/news) with a full mock-data fallback so it works with no API key configured. Non-stocks assets (crypto/bonds/FX/real estate) show an instant "not available for this asset class" message with no loading flash, since Finnhub's company-profile endpoint has no equivalent for those classes.

**Real bug caught and fixed during the build's own live verification, before it ever reached the independent verify pass:** the new popovers, positioned with plain CSS `absolute` inside the chart slot as originally designed, got clipped by the slot's own scroll boundary when tested in the cramped "1 + 2 grid" layout at a realistic ~1366×768 size (confirmed via `getBoundingClientRect()` — the insight panel's right edge extended 190px past its slot's right edge). Fixed by portaling both popovers into `document.body` with `position: fixed`, position computed from the trigger's real bounding rect and recomputed on scroll/resize — the exact same pattern `Tooltip.tsx` already uses to solve this identical class of problem. Re-verified clean afterward.

**Four minor gaps caught by the independent verify pass, all fixed before this commit:** (1) no test coverage for the new `profiles` table despite `candles`/`news` each having a dedicated describe block — added one covering cache-miss, round-trip, staleness, and upsert-not-duplicate; (2) no unit tests for the new `formatFinance.ts` despite this codebase's habit of thoroughly testing pure-logic files — added boundary tests at the exact M→B and B→T thresholds; (3) a dead i18n key (`assetInsight.industry`) defined in all 11 locales but never actually rendered with a label (the component shows industry as an unlabeled subtitle under the company name, matching this app's existing "ticker · full name" subtitle convention elsewhere) — removed the unused key rather than forcing an awkward label onto what already reads cleanly as a subtitle; (4) a stocks-class asset whose profile genuinely resolves to `null` (e.g. a delisted symbol on a real API call) fell through to blank content with no message — added a distinct `assetInsight.notFound` string (translated into all 11 locales) for this case, separate from the "wrong asset class" message.

**Verified:** `npm run typecheck`/`test` (94 tests, up from 85)/`build` all clean. Live in the browser: opened the search popover on a non-focused chart slot, selected a different symbol, confirmed only that slot changed; opened the insight panel on NVDA (stocks) and saw a full profile with the industry subtitle rendering cleanly with no label; switched to a non-stocks asset and confirmed the instant unavailable message; confirmed both popovers render fully on-screen (no clipping) in the "1 + 2 grid" layout at a cramped viewport size; confirmed clicking either new button still focuses its chart slot (no `stopPropagation` regression). i18n key-parity confirmed across all 11 locales.

## 2026-07-11 - Gave the workspace a protective minimum width against a widened dock pane (direct request, with screenshot)

**Built:** a screenshot showed that stretching the right-hand dock pane (Portfolio Risk/Options/Market News) all the way to its maximum width crushed the chart workspace down to an unreadably cramped size — toolbar rows wrapping onto multiple lines, price/volume labels stacking illegibly. Root cause: `.body`'s CSS grid gave the workspace column `minmax(0, 1fr)` — literally no protective minimum, so the dock's resize handle (already capped at 520px max) could still squeeze the workspace arbitrarily small depending on the window's overall width.

**Fix:** changed the workspace column to `minmax(700px, 1fr)` and added `overflow-x: auto` to `.body`, so the dock can still be dragged to its full 520px width, but the workspace can never be squeezed below a legible floor — on a narrower window that can't fit both, the trade-off is a small horizontal scrollbar instead of silently degrading the charts into illegibility.

**Verified:** live in the browser, dragged the dock resize handle to its exact maximum (520px, confirmed via `aria-valuenow`) at both a wide (1600px) and a moderately narrow (1280px) viewport. At 1600px the workspace easily clears the new floor (1006px, no scrollbar needed). At 1280px the workspace correctly holds at exactly 700px and the body gains a 14px horizontal overflow — and visually, the "1 + 2 grid" layout's secondary charts still render clean single-line toolbars with no wrapping, a large improvement over the reported cramped state. `npm run typecheck` clean; `npm test`/`build` deferred until the concurrently-running chart quick-switch feature (Phase 4) finishes, to avoid disturbing its in-progress native-module test state.

## 2026-07-11 - Made the RSI/MACD panel reachable via scroll in the "1 + 2 grid" layout (phase 5 of a reprioritized batch)

**Built:** changed `.chart-slot { overflow: hidden }` to `overflow-y: auto; overflow-x: hidden` in `layout.css`. This is a direct follow-up to the ticker-bleed fix from earlier today: that fix correctly stopped the secondary charts in the "1 + 2 grid" layout from visually bleeding into the ticker tape, but as a side effect it also silently clipped the RSI/MACD oscillator subpanel out of view with no way to reach it whenever those charts didn't have enough room — Will caught this immediately ("I can't scroll down to see the RSI chart on the 1+2"). The fix keeps the existing `.chart-wrap` min-height override in place (so the chart canvas itself stays a sane size) and just lets the rest of the column become scrollable instead of clipped.

**Verified:** confirmed live at the same ~1366x768 viewport used to diagnose the original bug — toggling RSI on a secondary chart in "1 + 2 grid" now produces a slot with 428px of content in a 306px box, genuinely scrollable, and scrolling down reveals the oscillator panel correctly. Re-confirmed the original ticker-bleed fix hasn't regressed (content still can't escape the slot's box). `npm run typecheck`/`build` clean.

## 2026-07-11 - Fixed the chart price scale getting stuck after switching symbols (direct request)

**Built:** a one-line fix in `PriceChart.tsx` for a real UX bug Will described: after manually dragging the price (vertical) scale on a chart, switching to a different symbol with a very different price range left the chart looking blank — the price scale stayed locked to the old symbol's range rather than re-fitting to the new data.

**Root cause:** the chart already re-fits its *time* axis on every data change (`timeScale().fitContent()`), but nothing reset the *price* axis. Dragging the price scale by hand turns off lightweight-charts' `autoScale` for the life of that chart instance, and the chart instance isn't recreated on a symbol switch (only re-fed new data), so a manually-adjusted scale stayed stuck on the old symbol's range indefinitely.

**Fix:** call `chart.priceScale('right').applyOptions({ autoScale: true })` alongside the existing `fitContent()` call, every time candle data is pushed — so autoScale is unconditionally re-enabled on every symbol/timeframe switch, regardless of any prior manual adjustment.

**Verified:** live in the browser, switched AMD ($36) → BTC ($64,230) → NVDA ($142.87) — roughly a 450x swing in price range — and confirmed the chart correctly re-fits its Y-axis to each symbol's own range every time, with no leftover scale from the previous symbol. `npm run typecheck`/`build` clean.

## 2026-07-11 - Portfolio risk grade and advice (phase 3 of 7)

**Built:** a new "Risk grade" section in the Portfolio Dashboard tab (both the per-portfolio pane and the aggregate Overview), sitting between the existing risk-stat grid and the AI Insights section. It reduces the 6 risk stats plus asset-class concentration into a single 0-100 score and a Conservative/Moderate/Elevated/High category, shown as a gradient meter with a marker, a plain-language description, narrative bullets for any notably strong or weak signal, and a handful of category-appropriate tips — explicitly framed throughout as a description of how much a portfolio tends to move, never a verdict.

- New `src/renderer/src/lib/riskGrade.ts`: reuses the existing `assessRisk()` thresholds for volatility/max drawdown/VaR/beta, adds a new diversification tone computed from asset-class concentration, and combines all five with weights (volatility/maxdd/diversification 2.5 each, VaR 1.5, beta 1.0 — deliberately the lowest, so a "typical" beta can never launder away real risk elsewhere). Sharpe and Sortino are deliberately excluded — they measure risk-*adjusted return quality*, not risk magnitude, and including them would let a turbulent-but-lucky portfolio read as "safe." A single-asset-class portfolio is floored at score 60 (at least "Elevated") before banding, since a brand-new single-stock position may not look volatile yet on paper but is still concentrated risk. The module is pure logic with zero hardcoded copy or i18n calls, mirroring how `riskAssessment.ts` already separates tone-computation from display text.
- New `PortfolioRiskAdvice.tsx`: the gradient meter is forced `direction: ltr` (a data axis that must never visually reverse under Arabic/Urdu), reuses the existing `--gain`/`--warn`/`--loss` tokens, and only surfaces bullets for signals that are notably good or bad (neutral ones are skipped so the section doesn't get noisy).
- Full `portfolio.riskAdvice` i18n block translated into all 11 locales in this same pass (normal volume, not deferred).

**Verified, unusually thoroughly given this is scoring logic people might actually rely on:** the independent verify pass hand-derived the weighted-score formula from scratch and recomputed every boundary test (24/25, 49/50, 74/75 band edges) against the actual thresholds in `riskAssessment.ts` and `portfolioBreakdown.ts`, rather than trusting the build agent's fixtures — confirmed every test asserts a genuinely correct result, not just a passing one. Also confirmed the concentration floor applies before banding (not after), the diversification percentage is read as 0-100 not 0-1, and Sharpe/Sortino are never referenced anywhere in the scoring code. No bugs found. `npm run typecheck`/`test` (85/85)/`build` all clean, re-run independently rather than trusted from the build pass. Live in the browser: a single-holding AAPL portfolio correctly floors to score 60 ("Elevated") with accurate narrative bullets and tips; meter renders correctly in both light/dark theme and stays left-to-right even switched to Arabic.

## 2026-07-11 - Fixed the "1 + 2 grid" layout bleeding through the bottom ticker (direct request, with screenshot)

**Built:** a CSS fix for a real, reproducible layout bug Will reported with a screenshot: in the "1 + 2 grid" chart layout (one focused chart on top, two smaller ones stacked below), the two bottom charts' hover readouts and volume bars visually bled straight through the ticker tape at the bottom of the window, producing illegible overlapping text.

**Root cause, confirmed by inspecting real computed layout (not guessed from reading CSS alone):** `.chart-wrap` has a hard `min-height: 200px` floor. That's fine when a chart slot gets the full workspace row height, but the "1 + 2 grid" layout's two secondary charts only get half the row (a 2-row CSS grid), and on a common 1366x768-class screen only about 110px is actually left for `.chart-wrap` after the symbol header and toolbar rows. The 200px floor forced those charts to render 90px taller than their slot's own box, and nothing in the ancestor chain (`.chart-slot`, `.chart-slot-row`, `.workspace`) clipped that overflow, so it painted straight through the ticker tape below (and even past the viewport's own bottom edge in the reproduction).

**Fix (`src/renderer/src/components/layout/layout.css`):**
- Added `overflow: hidden` to `.chart-slot` itself — the general, root-cause containment fix: whatever a slot's content wants to be, it can never again visually escape its own box into unrelated UI below it, regardless of which layout template or how short the window is.
- Added a scoped override lowering `.chart-wrap`'s floor to 100px specifically for the "1 + 2 grid" layout's two secondary slots (`.chart-slot-row-threeGrid .chart-slot:not(:nth-child(1)) .chart-wrap`), so those charts render at their intended smaller size instead of relying on the new clipping to silently crop them.

**Verified:** reproduced the exact bug first (resized the browser preview to 1366x768, confirmed via real `getBoundingClientRect()` measurements that the bottom charts' `.chart-wrap` boxes extended 90px past their slot and past the ticker, matching the screenshot exactly), then confirmed after the fix that both secondary charts render fully contained within their own slots with zero overlap. Also tested enabling RSI on a secondary chart in this same cramped scenario (adds an oscillator subpanel using a separate, globally-shared height setting) — confirmed it's now cleanly clipped from view rather than bleeding through, a reasonable degrade for an edge case the user didn't report. Checked all 4 other layout templates (1 chart, 2 charts, Focus + 1, 3 charts) at the same viewport size and confirmed none regressed. `npm run typecheck`/`build` both clean (pure CSS change, no test suite exercises this).

**Note:** did not run the native-module test suite for this fix, since another build cycle (Phase 3 of the current plan) was rebuilding `better-sqlite3` concurrently in the background and running `npm test` at the same time risks corrupting that in-flight ABI-flip; this change touches no JS/TS logic and no existing test references `layout.css`, so this was a deliberate, safe skip rather than an oversight.

**Flagged for later, not fixed now:** the not-yet-built chart-quick-switch/asset-insight-panel work (queued Phase 6) was designed assuming `.chart-slot` has no `overflow: hidden` ancestor, so its planned popovers can render via plain absolute positioning without clipping. That assumption is no longer true after this fix. When Phase 6 is implemented, its popovers will need to be re-verified against the live codebase at that time — likely by having them portal to `document.body` the same way `Tooltip.tsx` already does, rather than relying on unclipped local positioning.

## 2026-07-11 - Excel export: portfolio reports and the market data archive (phase 2 of 7)

**Built:** two real export flows on top of last phase's local database, both producing genuine `.xlsx` workbooks via `xlsx` (SheetJS) and Electron's native save dialog.
- **Portfolio report** — a new download-icon button in both `PortfolioPane.tsx`'s and `PortfolioOverviewPanel.tsx`'s headers (`ExportReportButton.tsx`, shared between the two) exports a 3-sheet workbook: Holdings (symbol/name/quantity/cost basis/current price/market value/P&L $ & %/weight %), Risk Stats (the 6 portfolio risk metrics), and Class Breakdown (asset class/market value/%). All the underlying numbers were already computed in the renderer; the main process only turns them into a workbook, it never recomputes anything.
- **Market data archive** — a new "Market data archive" section in the Customize panel, right after API keys, lets you export every candle and news article this app has actually stored locally (optionally narrowed to one symbol) into a 2-sheet workbook (Candles, News).
- New main-process module `src/main/exportData.ts` (workbook builders + a `dialog.showSaveDialog` wrapper that correctly distinguishes a genuine write from a user cancel) and two new `data:export*` IPC channels, following the exact same `ipcMain.handle`/preload-wrapper/`QiabApi` pattern the local database's channels already established.

**Real bug caught by the independent verify agent, not the build agent:** the market archive handler tagged every exported candle row with a single `(source, symbol)` summary's timeframe, but `getCandleHistoryForExport` had no timeframe filter — so a symbol cached under two timeframes (very common: `usePortfolioRiskStats` always caches a `'1Y'` series for portfolio math independent of whatever timeframe a chart is separately showing that same symbol at) had its full candle history exported twice, mislabeled under each timeframe. The verify agent reproduced this empirically against the real SQL (2+2 stored rows under `'1D'`/`'1Y'` produced 8 exported rows with swapped timestamps). Fixed by adding a genuine `timeframe` parameter to `getCandleHistoryForExport`'s `WHERE` clause and calling it per `(source, symbol, timeframe)` triple instead of per `(source, symbol)`.

**Also fixed while addressing the above:** the archive's free-text symbol input didn't normalize case, so typing a lowercase symbol would silently match nothing (stored symbols are uppercase) and export an empty archive rather than erroring — added `.toUpperCase()`.

**Dependency note:** `xlsx@0.18.5` (the only version npm's registry carries; SheetJS's own CDN has newer patched releases npm doesn't mirror) has known high-severity prototype-pollution/ReDoS advisories, but both live in the *parser* (`XLSX.read`); this feature only ever *writes* workbooks built from already-trusted local data, so the practical exposure here is effectively nil. Flagging for visibility rather than treating it as blocking, since swapping to a differently-sourced install would break this project's plain-`npm install` convention for no real safety gain.

**Verified:** `npm run typecheck`/`test` (78/78)/`build` all clean (after the timeframe fix and its accompanying test update). Live in the browser: opened a portfolio and the Customize panel, confirmed both new buttons/section render exactly where designed, and confirmed both correctly show a graceful "Export failed" status (rather than throwing) when clicked outside a real Electron window, `window.api` being unavailable there — the same defensive `window.api?.` pattern already established for every other IPC call in this codebase. No console errors.

**Not yet verified end-to-end**: an actual save-dialog round-trip (real file written, opened in a spreadsheet application, contents checked) needs the real Electron app, not the browser preview — recommend a quick manual pass in `npm run dev`'s real Electron window before the next release.

## 2026-07-11 - A real local SQLite database, replacing zero persistence (phase 1 of 5, direct request)

**Built:** the app now owns a real local database (`better-sqlite3`, an embedded SQL engine) at `app.getPath('userData')/quant-in-a-box.db`, instead of the previous reality where every chart, every portfolio's risk calculation, and the benchmark comparison feature all fired independent live API calls on every single render with zero caching or persistence anywhere. This was flagged directly by Will after asking whether an underlying database existed; he explicitly asked for a real, owned, exportable store rather than a disposable cache ("we're already calling it, we might as well own it") — export comes in the next pass.

- `candles`/`news` tables, both keyed so repeated fetches naturally upsert rather than duplicate (`PRIMARY KEY (source, symbol, timeframe, time)` on candles) — old bars are never deleted, so the database genuinely accumulates a growing historical archive across every symbol ever viewed, not a rolling window.
- New main-process module `src/main/localDb.ts` (SQLite only works in the main process; the renderer is sandboxed), wired through 4 new `data:*` IPC channels mirroring the existing `ai:*`/`settings:*` pattern exactly.
- `dataService.ts`'s live-fetch methods now check the database first (skipping the network call entirely on a fresh-enough hit) and write back on a successful live fetch — TTLs scaled to how fast each timeframe actually goes stale (5 min for 1D/1W candles, up to 60 min for 1Y/5Y, 15 min for news). Critically, a mock-data fallback (no key, or a failed live call) is **never** written to the database, so a transient rate-limit failure can't get pinned as "real" data for the TTL window — the next call retries live.
- No CI changes needed: this project's packaging already had the exact `electron-builder install-app-deps` step meant for rebuilding native modules like this per-platform.

**Real bug caught during my own live verification, not by either build/verify agent:** `better-sqlite3` is a plain (non-N-API) native addon, so its compiled binary only loads under the exact Node ABI it was built for — `vitest` runs under the host's plain Node, while `electron-builder install-app-deps` rebuilds it for Electron's ABI. The two agents' fix (npm's `pretest`/`posttest` hooks flipping the binary's ABI back and forth around the test run) has a real gap: npm only runs `posttest` if `test` exits zero, so a genuine test *failure* would leave the binary rebuilt for host Node and silently break `npm run dev`/`build` until someone reran `npm install`. Replaced both hooks with one small cross-platform script (`scripts/test-with-rebuild.js`) that rebuilds for host Node, runs vitest, and rebuilds back to Electron's ABI unconditionally, then re-exits with vitest's real exit code — so a test failure is still reported correctly to CI, but never leaves the repo unable to launch Electron.

**Second real bug caught during my own live verification**: the new `window.api.getCachedCandles(...)`/`getCachedNews(...)` calls were written without the `window.api?.` optional chaining this codebase already uses everywhere else for IPC calls (e.g. `window.api?.checkAiAvailability()`). `window.api` only exists inside a real Electron renderer (injected by the preload bridge) — calling it directly would throw the moment a Finnhub/TwelveData key is configured in any context without that bridge. Confirmed live: with a fake Finnhub key set and no `window.api` present, the unpatched code would have thrown; after adding the same defensive `?.` pattern used elsewhere, the chart rendered correctly with zero console errors, falling through cleanly to a live-fetch attempt (and mock on failure) exactly as before this change.

**Verified:** `npm run typecheck`/`test` (78/78, 16 new)/`build` all clean, plus a genuine from-scratch dependency install confirming `better-sqlite3` rebuilds cleanly for Electron's ABI via the existing postinstall step. Live in the browser: confirmed the previously-crashing scenario (a configured live-data key with no Electron preload present) now renders the chart correctly with no console errors.

**Not yet verified end-to-end**: the actual database read/write/staleness-check behavior needs a real running Electron app (IPC to a genuine main process, not just the Vite dev server in a browser tab) — recommend a quick manual check in the packaged/dev Electron app: view a symbol, reload, confirm no new network call fires within the TTL window, and inspect the `.db` file's row count growing as more symbols are viewed.

## 2026-07-11 - Fixed recurring tooltip-clipping bug at the root (direct request)

**Built:** rewrote the shared `Tooltip.tsx` to render its bubble through a React portal into `document.body` at a fixed viewport position, instead of as a plain `position: absolute` child of its trigger. This was a real, recurring bug: any ancestor with `overflow: hidden` (stat tiles, scrollable dock/panel bodies) clipped the tooltip regardless of its `z-index`, since overflow clipping and stacking order are separate CSS mechanisms. Earlier passes patched this one container at a time (a `:has(.tooltip-bubble)` override on stat tiles, `side="right"` placement on dock card buttons, removing overflow from `OverlayPanel`) rather than fixing the shared component, so any new container built since kept the same underlying risk. A portal sidesteps the problem entirely: the tooltip is no longer a descendant of whatever might clip it, so no future container can reintroduce this bug.

- Position is computed from the trigger's `getBoundingClientRect()` on show, converted to a single anchor point per `side` (top/bottom/left/right), with the existing centering handled via a static CSS `transform` instead of parent-relative `calc(100% + Npx)` offsets (which only worked because the tooltip used to share a positioning context with its trigger).
- Added a scroll/resize listener while a tooltip is visible so it can't go stale-positioned if the page scrolls mid-hover, a new failure mode a portaled, viewport-fixed tooltip could otherwise introduce.
- `z-index: 200` bumped to `1000` (highest in the app is otherwise 30), so the tooltip is unambiguously always on top now that it renders as a body-level sibling rather than nested inside whatever local stacking context its trigger lived in.
- Removed the now-dead `.stat-tile:has(.tooltip-bubble) { overflow: visible }` rule — it can never match again since the tooltip no longer lives inside `.stat-tile` at all.

**Verified:** `npm run typecheck`/`test` (62/62)/`build` all clean. Live in the browser: confirmed the tooltip bubble renders as a direct child of `document.body` (not nested under the stat tile), with `position: fixed` and `z-index: 1000`, fully visible and un-clipped.

## 2026-07-11 - Portfolio dashboard: combined "All portfolios" overview (phase 2 of 2, pass 4 of 4, last)

**Built:** a new Rail entry point ("All portfolios," next to the existing per-portfolio quick-launch dots, same `portfolios.length > 0` gating) opening a standalone panel that merges every saved portfolio into one combined view: a real quantity-weighted merge (the same symbol held in two different portfolios becomes one row with summed quantity and a correctly blended average cost), reusing the exact Holdings/Dashboard tab composition every per-portfolio panel already has, so the class breakdown, holdings ranking, benchmark comparison, risk stats, and AI insights all Just Work here too without a third copy of any of that logic. Last of four sequenced passes for the portfolio dashboard feature, and the last of the two features from the original request (chart styles + portfolio dashboard).

- New `mergePortfolioHoldings(portfolios)` in `lib/portfolioHoldings.ts` generalizes the exact same quantity-weighted cost-basis blend formula `addPosition` already uses when you add to an existing holding in the same portfolio, extended from 2 contributions to N. 7 new unit tests, including one that computes the expected value with the literal same arithmetic the app itself uses (not a hand-typed decimal), so the test can't silently drift from the real formula.
- New `HoldingsTable.tsx` extracted from the per-portfolio panel's existing table (same columns/formatting), now shared: editable (with a remove button) when used per-portfolio, read-only (no remove button, since removing a position that's blended across multiple source portfolios is ambiguous) when used in the new combined view.
- New `PortfolioOverviewPanel.tsx`, built on the same `OverlayPanel` every other full-screen panel in this app already uses, mounts the same `PortfolioDashboardTab` component the per-portfolio Dashboard tab uses (this is the entire payoff of extracting that composition three passes ago), so a future change to any of the breakdown/benchmark logic only needs to happen in one place to show up everywhere.
- `overviewOpen` is a plain, non-persisted boolean (mirroring the existing Academy/Customize open-state pattern exactly) — it's just "is this panel currently showing," not something that needs to survive a restart.
- 4 new i18n keys (reusing the existing Holdings/Dashboard tab labels rather than duplicating them), translated into all 10 other languages, key-parity script confirms 298/298 across all 11 locales.

**Verified:** `npm run typecheck`/`test` (62/62, 7 new)/`build` all clean. Live in the browser, fresh tab: seeded two portfolios both holding AAPL at different quantity/cost, opened the new panel, and confirmed the merged row showed qty 15 at a $160.00 average cost, exactly matching `(10×150 + 5×180)/15` by hand. Confirmed the merged table has no remove buttons (read-only, as intended) while the same holding still shows a working remove button inside its own portfolio's panel. Confirmed the Dashboard tab's class breakdown, holdings ranking, and benchmark comparison all render correctly against the merged data, with "Portfolio Analytics" matching the comparison table's "Your portfolio" column exactly. Checked the zero-portfolio state (entry point correctly hidden, same as the existing per-portfolio rail dots) and the single-portfolio state (renders correctly with no merge artifacts, 100% weight on the one holding).

## 2026-07-11 - Portfolio dashboard: compare against a model portfolio (phase 2 of 2, pass 3 of 4)

**Built:** a "Compare to a model portfolio" section in the Dashboard tab, letting you check your holdings' risk profile against a synthetic "typical fund," since there's no real external data source for actual ETF/fund holdings in this app. Three named presets to pick from (per Will's own choice of "a few selectable model portfolios" over a single fixed benchmark or a purely qualitative description): Conservative (15% stocks / 55% bonds / 2% crypto / 20% FX incl. gold / 8% real estate, blue-chip-only equities), Balanced (40/30/10/10/10, a classic diversified core), Growth (45% stocks / 10% bonds / 30% crypto / 5% FX / 10% real estate, equity- and crypto-heavy). Every symbol in every preset is a real symbol already curated in this app's own asset universe, not invented data.

- `lib/usePortfolioRiskStats.ts` split into a core `useWeightedRiskStats(weightedHoldings)` engine and a thin `usePortfolioRiskStats(positions)` wrapper for real holdings, so your real portfolio and a synthetic model portfolio both run through the exact same fetch/blend/stats pipeline, making the comparison genuinely apples-to-apples rather than two different calculations. The engine now also exposes the cumulative value series it was already computing internally (previously only used for max drawdown), so a comparison chart has real data to plot.
- New `data/modelPortfolios.ts` (+ 7 unit tests asserting each preset's weights sum to 1.0, every symbol resolves in this app's real asset data, and the three presets' relative bond/crypto weighting stays in the correct order as a guard against a future edit accidentally inverting their risk profiles).
- New `BenchmarkSelector` (a 3-way segmented control), `BenchmarkCompareChart` (a hand-rolled canvas overlay of your portfolio's value curve against the selected preset's, following this app's existing oscillator-panel canvas pattern, no new charting dependency), and `BenchmarkCompareStats` (side-by-side risk stats, reusing the exact same formatting the stat tiles already use elsewhere).
- The benchmark selection is local state, not shared globally, so a later pass can mount this same Dashboard composition a second time (for a cross-portfolio overview) with its own independent selection.
- 12 new i18n keys (the compare section's copy plus each preset's name/description), translated into all 10 other languages, key-parity script confirms 294/294 across all 11 locales.

**Verified:** `npm run typecheck`/`test` (55/55, 7 new)/`build` all clean. Live in the browser, fresh tab: a 3-position test portfolio (AAPL/BTC/AGG, crypto-heavy) showed a negative Sharpe/Sortino for "your portfolio" against a clearly positive Sharpe/Sortino for the Balanced preset, and switching the selector to Conservative correctly re-fetched and produced different, plausible numbers while "your portfolio" column stayed fixed. Directly inspected the comparison chart's canvas pixel data to confirm two genuinely distinct colors are drawn (not a single overlapping line or a rendering no-op). RTL (Arabic) checked with no console errors.

## 2026-07-11 - Portfolio dashboard: breakdown by asset class and by holding (phase 2 of 2, pass 2 of 4)

**Built:** each portfolio panel now has Holdings/Dashboard tabs. Holdings keeps today's add-position form, summary tiles, and table exactly as before. The new Dashboard tab adds two visual breakdowns above the existing risk-stat grid and AI Insights (both moved here unchanged): a "Breakdown by asset class" horizontal stacked bar (stocks/crypto/bonds/FX/real estate, with a legend showing $ value and %), and a "Breakdown by holding" ranked list (your top 8 positions by market value, with anything beyond that folded into one "Other (N)" row and a "show all" toggle to expand it).

- New `lib/portfolioBreakdown.ts`: `computeAssetClassBreakdown()` groups resolved holdings by asset class in a fixed stocks→crypto→bonds→fx→re order (so colors/positions stay stable once a benchmark comparison is added in a later pass), omitting any class you don't hold rather than showing a 0%-width slice. `rankHoldingsWithOther()` sorts by weight and folds anything past the top 8 into a single combined bucket, never silently dropping data (the "show all" toggle always gets you back to every individual row). 9 new unit tests.
- New `lib/assetClassStyle.ts`: a fixed color + icon per asset class, deliberately distinct from the `--gain`/`--loss`/`--warn` tokens so a class's color never reads as "up," "down," or "a warning."
- New hand-rolled components (`ClassBreakdownBar.tsx`, `HoldingsRankBar.tsx`) — plain flex/CSS, no new charting dependency, matching this project's established preference for hand-rolling simple visuals.
- New `PortfolioDashboardTab.tsx` is the shared composition (breakdowns + the existing risk grid + AI Insights) that a later pass will also reuse for a combined cross-portfolio overview, so that feature doesn't need a third copy of the same pipeline.
- 10 new i18n keys, translated into all 10 other languages, key-parity script confirms 282/282 across all 11 locales.

**Verified:** `npm run typecheck`/`test` (48/48, 9 new)/`build` all clean. Live in the browser, in a fresh tab (an initial check in a long-reused tab showed a stale React error from before these changes landed via HMR, which a fresh tab confirmed was leftover cache, not a real bug, matching a pattern seen repeatedly earlier in this project): a 10-position diversified portfolio correctly showed 5 asset-class percentages summing to 100% and an 8-plus-"Other (2)" holdings ranking that expanded to all 10 individual rows on "show all" and collapsed back correctly; a single-position portfolio showed one 100% class slice with no phantom zero-width segments for the other four; an empty portfolio showed the existing empty-state message with no NaN or broken math.

## 2026-07-11 - Portfolio dashboard groundwork: extracted shared holdings/risk-stats logic (phase 2 of 2, pass 1 of 4, pure refactor)

**Built:** nothing user-visible. This is the foundation pass for a portfolio dashboard feature (breakdown by asset class, breakdown by holding, a benchmark comparison against a few model portfolios), which needs the existing per-portfolio quant pipeline reusable from more than one place before any of that can be built without a third copy-paste. Pulled the holdings-resolution and risk-stats logic that lived inline inside `PortfolioPane.tsx` (and was independently duplicated a second time in `RiskCard.tsx` just for its fake benchmark asset) out into shared, tested library code.

- New `lib/portfolioHoldings.ts`: a pure `resolveHoldingRows(positions, assets?)` function, lifted verbatim from `PortfolioPane.tsx`'s old inline row-join (same market value/P&L/weight% formulas, same "skip a position whose symbol doesn't resolve" behavior). 4 new unit tests.
- New `lib/usePortfolioRiskStats.ts`: the async stats-fetching engine (candles → weighted returns → Sharpe/Sortino/Vol/VaR/MaxDD/Beta), lifted verbatim from `PortfolioPane.tsx`'s old inline effect, same stale-fetch guard so switching portfolios mid-fetch can't leave a stale result on screen.
- `mockData.ts` gains one exported `SPX_PROXY_ASSET` (the fake broad-market-proxy benchmark used for beta), replacing two independent inline copies that had drifted into existing separately in both `PortfolioPane.tsx` and `RiskCard.tsx`.
- `PortfolioPane.tsx` now calls these two extracted functions instead of computing everything inline; its rendered output (summary tiles, holdings table, the 6-tile risk-stat grid, AI Insights) is unchanged, since only the "where the computation lives" moved, not the computation itself.

**Verified:** `npm run typecheck`/`test` (39/39, 4 new)/`build` all clean. Live in the browser: seeded a test portfolio (AAPL + BTC) and confirmed the panel renders identically to before (correct market values, P&L, weights, and all 6 risk stats populated) with zero console errors in a fresh tab. Grepped the whole `src/` tree to confirm exactly one `SPXPROXY`-shaped benchmark definition now exists, not two independently-drifting copies.

**Why this pass exists on its own:** a pure-refactor pass with an explicit "zero visible behavior change" bar is the easiest kind of change to verify in isolation and the easiest to bisect if something regresses later, before any real new UI gets layered on top of it in the next three passes.

## 2026-07-11 - Chart style switching: Candles, Bars, Line, Area, Baseline (direct request, phase 1 of 2)

**Built:** a second segmented control in each chart slot's toolbar letting you switch the main price chart between 5 styles: Candles (existing default), Bars (OHLC), Line (smoothed), Area (smoothed with a gradient fill below the line), and Baseline (two-tone gain/loss area relative to the period's starting price). First of two features from a single request (the second is a portfolio dashboard, built as a separate follow-up pass). No new dependency was needed: `lightweight-charts` (already used for candlesticks) natively ships all 5 series types, including built-in line smoothing (`LineType.Curved`) and gradient area fills.

- Chart style is a per-chart-slot setting, exactly like timeframe and indicators already are, persisted the same way (`qiab:chartSlots:v1`), with the same guarded-fallback-to-default behavior if a saved slot is missing the field.
- `PriceChart.tsx`'s chart-creation effect (which already fully tears down and rebuilds the chart on a theme change) now also rebuilds on a style change, reusing that same proven path rather than a more fragile in-place series swap. The simplified styles (Line/Area/Baseline) are colored by whole-period trend (green if the visible range closed up, red if down), matching how Robinhood/Apple Stocks/Google Finance color simplified charts, and recomputed live if you change timeframe while already on one of these styles.
- Every existing overlay (MA20/MA50/Bollinger as separate line series, RSI/MACD in the fully separate oscillator canvas) needed zero changes and keeps working identically across all 5 styles, by construction: they were already decoupled from the main series' type.
- 5 new i18n keys (`workspace.chartStyle.*`), translated into all 10 other languages, key-parity script confirms 272/272 across all 11 locales.

**Verified:** `npm run typecheck`/`test` (35/35)/`build` all clean. Live in the browser: cycled all 5 styles with MA20 and separately RSI toggled on, confirming overlays and the oscillator panel are unaffected in every style; hover readout correctly kept showing full O/H/L/C/Vol/MA data on non-candle styles; toggled dark/light theme in a non-candle style and confirmed colors updated correctly; set 3 different styles on 3 different chart slots simultaneously (a 3-chart layout) and confirmed full independence; reloaded to confirm per-slot persistence, and separately stripped the `chartStyle` field from saved data to confirm a clean fallback to Candles with no crash.

**Real bug caught and fixed:** the plan's own verification checklist called for checking the toolbar under a longer-translated locale in the narrowest layout, and it found a real one. In French, in the 3-chart layout's narrowest column, the new style picker's longest label ("Ligne de base") pushed the whole control about 18px past its chart slot's right edge, which (since nothing in that ancestry clips overflow) would have visually spilled into the Dock sidebar. Root cause: `.segmented` had no `flex-wrap`, so when its buttons' combined width exceeded the available column width, the browser let the control overflow horizontally rather than wrap. Fixed by adding `flex-wrap: wrap; min-width: 0;` to the shared `.segmented` class (used by every segmented control in the app, not just this one), so any segmented control that doesn't fit its available width now wraps its buttons onto an extra internal row instead of spilling outside its container. Re-verified: French now wraps cleanly with no overflow, and confirmed the fix doesn't introduce unwanted wrapping in the normal wide English/single-chart case.

## 2026-07-11 - Portfolio icon/color quick-buttons on the Rail (direct request, feature 4 of 4, last)

**Built:** each saved portfolio can now be given its own icon and color, shown as a one-click quick-launch button in the Rail sidebar, similar to how Claude Projects lets you pick an icon and color to tell projects apart at a glance. Last of the four features from the original request; lowest risk of the batch, purely additive with no dependency on the other three.

- `Portfolio` (in `types/market.ts`) gains optional `icon?`/`color?` fields. `loadPortfolios()` does no shape validation beyond `Array.isArray`, so existing saved portfolios simply lack these fields and fall back to one shared default (a portfolio icon in blue) at render time; no migration needed.
- New `lib/portfolioStyle.ts`: 8 curated icons (reusing existing `Icons.tsx` exports, no new SVGs needed) and 9 curated colors, deliberately distinct hues from `--gain`/`--loss`/`--warn` in `theme.css` so a portfolio's color never reads as "up," "down," or "needs attention." Icon and color lists are kept at different lengths on purpose, so `defaultStyleForPortfolio(index)` (used when a new portfolio is created going forward) doesn't repeat the same icon+color pairing until 72 portfolios in, rather than every 8.
- `AppStateContext.createPortfolio` now assigns a style from `defaultStyleForPortfolio` to every newly created portfolio; a new `updatePortfolioStyle(id, { icon?, color? })` mutator mirrors `renamePortfolio`'s shape.
- New `PortfolioStylePicker.tsx`: an icon-grid + color-grid popover, reusing `PortfolioPicker.tsx`'s existing click-outside-to-close pattern. Wired into `CustomizePanel.tsx`'s existing portfolio rename/delete row via a small swatch trigger button, and into `Rail.tsx` as a new `.rail-portfolios` block (one button per saved portfolio, mirroring the existing `.rail-watch` watchlist-dot pattern) that opens that portfolio directly on click.
- 12 new i18n keys under `customize.portfolioStyle.*` (trigger label, icon/color grid headings, a per-color "Color N" label, and 8 per-icon labels), translated into all 10 other languages, key-parity script confirms 267/267 across all 11 locales.

**Verified:** `npm run typecheck`/`test` (35/35)/`build` all clean. Live in the browser: seeded two legacy-style portfolios with no `icon`/`color` fields (simulating pre-existing saved data) and confirmed both correctly rendered the same shared default icon/color with no migration or crash. Opened the style picker for one, switched its icon to Crypto and its color to the 4th swatch, and confirmed the change persisted to `localStorage`, survived a reload, and updated its Rail dot immediately, while the untouched second portfolio kept the shared default. Created a third portfolio through the normal "new portfolio" flow and confirmed it was auto-assigned the next icon/color pair in the cycle (`bond`/violet, index 2), distinct from both the legacy portfolios' styles, confirming `defaultStyleForPortfolio` only applies going forward and never overwrites existing portfolios. Clicked a Rail portfolio dot and confirmed it opens that exact portfolio.

## 2026-07-11 - Resizable dashboard: drag the dock width and the indicator panel height (direct request, feature 3 of 4)

**Built:** two draggable dividers, both axes requested up front. A vertical handle between the Rail/Workspace area and the Dock lets you widen or narrow the Risk/Options/News sidebar (clamped 260-520px, default 320px, matching the existing fixed width). A horizontal handle inside each chart slot, between the price chart and the RSI/MACD subpanel, lets you resize that panel's height (clamped 80-260px, default 108px, matching the previous fixed height). Built on top of Feature 2's already-decomposed `ChartSlot.tsx` so the layout markup only needed touching once.

- New `components/layout/ResizeHandle.tsx`: a small hand-rolled divider (no splitter library in this repo, and `motion/react`'s drag controls are built for list-reordering, not dimension-dragging). Pointer-drag via native `pointerdown`/`pointermove`/`pointerup` on `window` (not React's synthetic events, so tracking continues outside the handle's own bounding box), plus full keyboard support (`role="separator"`, `tabIndex={0}`, `aria-valuenow`/`min`/`max`, arrow-key stepping in 16px increments) built in proactively given how many keyboard-unreachable controls this session's earlier adversarial reviews had already caught elsewhere in this app.
- Two new persisted `AppStateContext` fields (`dockWidthPx`, `oscillatorHeightPx`), same clamp-on-load/save-on-change pattern as every other persisted field in this file. `layout.css`'s `.body` grid and `.subpanel` height, both previously fixed values, now read from these via CSS custom properties.
- 2 new i18n keys (`workspace.resizeDock`/`resizeOscillator`, used as each handle's `aria-label`), translated into all 10 other languages, key-parity script confirms 255/255 across all 11 locales.

**Verified:** `npm run typecheck`/`test` (35/35)/`build` all clean. Live in the browser (1440x900, since the preview pane's default viewport sits below this app's own 900px responsive breakpoint that hides the Dock and horizontal handle entirely): dragged the dock-width handle, confirmed the CSS variable and `localStorage` both updated and survived a reload; dragged the oscillator-height handle down to its 80px floor, confirmed it clamped rather than going negative; confirmed both handles' max clamps (520px/260px) hold under sustained input. Tabbed to each handle and confirmed keyboard arrow-key operation works and respects the same clamps, once re-tested with real spaced-out keydown events (a first attempt firing 15 events in one synchronous loop produced a misleadingly small result: React batches multiple `setState` calls made from the same stale closure before it can re-render between them, which isn't how a real held arrow key or repeated real click actually behaves).

**Real bug caught and fixed, RTL-specific:** the plan's own verification checklist called for testing `dir="rtl"` behavior, and it turned out to matter. CSS Grid physically mirrors column order under `dir="rtl"`, so the Dock ends up on the *left* instead of the right, but `ResizeHandle`'s `invert` prop (which encodes "the controlled pane sits after the handle") was hardcoded and never accounted for that flip. Confirmed empirically in Arabic: dragging the dock handle toward the now-left-side Dock grew it instead of shrinking it, backwards from the physical drag direction. Fixed by computing an `effectiveInvert` that flips the horizontal axis's invert flag when `document.documentElement.dir === 'rtl'` (vertical resize is unaffected, since RTL only mirrors inline/horizontal direction). Re-verified in both directions: Arabic now shrinks the dock when dragging toward it and grows it when dragging away; English is unchanged from before the fix.

## 2026-07-11 - Multi-chart layout templates: 1/2/3 independently-assigned charts (direct request, feature 2 of 4, built out of order for risk isolation)

**Built:** 5 layout templates ("1 chart," "2 charts," "Focus + 1," "3 charts," "1 + 2 grid") letting the main workspace show 1-3 charts side by side, each independently watching its own symbol, timeframe, and indicator set. This was the second feature tackled from the batch (not the second requested) since it's the one that changes shared global state semantics rather than purely adding fields, and there's zero automated test coverage on this surface (the 35 passing vitest tests are all pure-function `lib/` tests) - isolating it made a regression easy to bisect instead of buried in a larger diff, per a plan Will approved up front.

- **State model**: `AppStateContext` now holds a fixed 3-entry `chartSlots: ChartSlotState[]` array (`{id, symbol, timeframe, indicators}`) plus `layoutTemplate` and `focusedSlotId`. The slot count never changes, only how many are *visible* per template, so switching templates never resets a slot's independent state.
- **The key design choice that kept this contained**: the existing `symbol`/`timeframe`/`indicators`/`selectSymbol`/`setTimeframe`/`toggleIndicator` fields stay in `AppState` under the same names, just reimplemented as thin aliases for "whichever chart slot is currently focused." Every existing consumer (`RiskCard`, `OptionsCard`, `useNewsFeed`, `DockCardOverlay`, Topbar's global search and asset-class chips) needed **zero code changes** - confirmed by grepping every `useAppState()` call site touching these fields before starting, and by typecheck staying clean throughout.
- **Component split**: `Workspace.tsx` is now a thin container (template picker + a row of visible slots), and a new `ChartSlot.tsx` holds what used to be the entire `Workspace.tsx` body, parameterized by `slotId` instead of reading top-level state directly - mirroring the existing `PortfolioWorkspace.tsx` → `PortfolioPane.tsx` pattern already in this codebase for "N independent instances, each bound to its own id." `PriceChart.tsx`/`OscillatorPanel.tsx` needed zero changes; both were already pure, prop-driven, and multi-instance-safe (each with its own `ResizeObserver`).
- Clicking anywhere in a chart slot (its header, timeframe buttons, indicator chips, or the chart itself) focuses that slot, so Dock cards (Risk/Options/News) always reflect whichever chart you're actively working with - a deliberate simplification over making Risk/Options/News each aware of all 2-3 open symbols at once, which would need its own switcher UI in an already-fixed-width sidebar.
- Persisted to three new `localStorage` keys (`qiab:layoutTemplate:v1`, `qiab:chartSlots:v1`, `qiab:focusedSlot:v1`), following the exact load/save pattern used everywhere else in this file. Slot 0 keeps today's exact startup default (first curated stock) so existing users see no change until they actually switch templates; slots 1/2 default to BTC and US10Y so a first-time multi-chart view shows genuinely different charts, not the same one three times.
- 5 new i18n keys under `workspace.template.*`, translated into all 10 other languages, key-parity script confirms 253/253 across all 11 locales.

**Verified:** `npm run typecheck`/`test` (35/35)/`build` all clean. Restarted the dev server fresh before browser verification (this session's established precaution given how much had changed) and confirmed a genuinely clean console via a fresh browser tab (an earlier stale-cache scare from a reused tab's console history turned out to be leftover entries from before the restart, not a live error - same false-alarm pattern seen earlier this session, re-confirmed rather than assumed).

Live in the browser: default single-chart view is pixel-for-pixel the same as before (same default symbol, same layout) aside from the new template picker row. Switched through all 5 templates and confirmed each renders the correct number of charts in the correct arrangement (measured via `getBoundingClientRect`: two equal ~50/50 columns, a ~68/32 focus split, three equal ~33% columns, and a grid with one full-width chart on top and two below it). Set one slot's timeframe to 1Y and toggled RSI on a different slot, confirmed each stayed fully independent. Reloaded the page and confirmed the template, both slots' symbols, timeframes, and indicator states all survived intact.

**Caught and fixed a testing-methodology mistake, not an app bug:** an early check of "click a chart to focus it, then use Topbar search" appeared to update the wrong (unfocused) slot. Investigating properly (redoing the sequence with explicit waits after every state-changing click) showed this was the same same-tick "React hasn't flushed yet" artifact already documented earlier this session, not a real aliasing bug - repeating the exact sequence with proper waits confirmed Topbar search, the asset-class chips, and the watchlist all correctly and dynamically target whichever slot is focused at the time, in both directions.

## 2026-07-11 - Runtime-configurable API keys: Finnhub, TwelveData, Anthropic (direct request, feature 1 of 4)

**Built:** a new "Data & API keys" section in Customize where you paste in your own Finnhub, TwelveData, and Anthropic keys, no `.env` editing or rebuild required. First of four features from a single request (multi-chart layout, resize, and portfolio icon/color quick-buttons are separate follow-up passes, each verified and committed on its own per a plan Will approved up front).

- **Finnhub, made reactive**: `data/dataService.ts`'s `dataService` was previously a frozen singleton chosen once at import time from a build-time `VITE_FINNHUB_API_KEY` env var. New `data/apiKeyStore.ts` persists UI-entered keys to `localStorage` (falling back to the env var so existing `.env` setups keep working); `finnhubAdapter.ts`'s key lookup and `dataService.ts`'s service selection both now re-check the current key on every call instead of once at startup, so saving or clearing a key in Customize takes effect immediately.
- **TwelveData, built from scratch**: a real new `data/twelveDataAdapter.ts` (this previously had zero implementation anywhere in the app, just a type declaration and a CSP allowance). Wired in as the preferred live source specifically for bonds/FX (Finnhub's free tier is thin there, matching the original `.env.example` framing), falling back to Finnhub or mock otherwise.
- **Anthropic, kept main-process-only**: this key is more sensitive (an LLM API key, not a market-data quote key), and the app already has an established invariant that it never reach the renderer/devtools. New `main/secureSettings.ts` uses Electron's built-in `safeStorage` (no new dependency) to encrypt the key to a file under `app.getPath('userData')`, decrypted once into memory at startup with the old `.env`'s `ANTHROPIC_API_KEY` as a fallback. Three new IPC handlers (`settings:getAnthropicKeyStatus`/`setAnthropicKey`/`clearAnthropicKey`, the status check returns a boolean only, never the key itself) plumbed through the existing thin `preload` bridge pattern.
- A `settingsVersion` counter in `AppStateContext` (bumped after any key save/clear) is wired into `PortfolioPane`'s existing one-shot AI-availability check, so a key saved while a Portfolio pane is already open is picked up without needing to close and reopen it.
- 12 new i18n keys under `customize.apiKeys.*`, translated into all 10 other languages, key-parity script confirms 248/248 across all 11 locales.

**Verified:** `npm run typecheck`/`test` (35/35)/`build` all clean. Live in the browser: saved a (deliberately invalid) Finnhub key via Customize, confirmed the status line flipped to "Live data active," then confirmed via an instrumented `fetch()` that `dataService.getCandles()` genuinely attempted a real request to `finnhub.io` using that exact key (not just a UI cosmetic change), and correctly fell back to generated mock candles when the fake key was rejected, so the UI never breaks on a bad key. Repeated the same check for a bonds-class asset with only a TwelveData key set, confirming the request correctly routed to `api.twelvedata.com` instead of Finnhub. Confirmed the renderer bundle contains no Anthropic key material, no `safeStorage`/`encryptString` internals, and no reference to the main-process-only `saveAnthropicKey`/`clearAnthropicKey`/`getAnthropicKey` functions (only the expected `getAnthropicKeyStatus` IPC method name, matching how the existing `checkAiAvailability`/`getPortfolioInsights` names already appeared pre-existing), preserving the security invariant this app has documented since the original AI-insights feature. Exercised the full Anthropic save/status/clear UI round-trip by mocking `window.api` (the real preload bridge only exists inside a running Electron window, not the raw browser dev-server preview; same constraint noted for the original AI-insights feature).

**Not yet verified end-to-end:** the `secureSettings.ts` `safeStorage` encrypt/decrypt/file-persistence logic itself hasn't been exercised inside a real running Electron process in this pass (only via code review plus the mocked-IPC UI test above). Recommend a quick manual check in the packaged/dev Electron app: paste a real Anthropic key, save, confirm "Configured," restart the app, confirm it's still configured.

**Fixed along the way:** `tsconfig.web.json`'s `include` list needed `src/main/secureSettings.ts` added explicitly. `aiInsights.ts` is type-only-imported by `preload/index.d.ts` (itself in the web project), and once `aiInsights.ts` gained a real (non-type) import of the new file, TypeScript's composite-project validation required that new file to be explicitly listed too.

## 2026-07-11 - Real license (PolyForm Noncommercial) + "Why This Exists" README section (direct request)

**Built:**
- Added a real `LICENSE` file: PolyForm Noncommercial License 1.0.0, matching Will's `gitsidian` repo's precedent and his explicit ask ("public but no commercial use without permission"). `package.json` now sets `"license": "SEE LICENSE IN LICENSE"` (standard npm convention pointing at the file), matching gitsidian's own package.json. Updated README's License section and license badge to match; removed the earlier "private, all rights reserved" framing now that a real license is in place.
- Added a "Why This Exists" section to the README, up front right after the intro: this app was built for Will's girlfriend, who wants to invest but doesn't yet speak the domain's jargon, so it's designed as an educational tool first. It's also fully usable for real portfolio analysis (enter actual holdings, get the same statistical methodologies professional analysts use), with the explicit, deliberate limitation that it never places trades (analysis and education only, no brokerage connection or buy/sell button). Includes a direct, unambiguous "not financial advice, your decisions are your own responsibility" statement, and a warm closing note linking to the Support section.

**Verified:** `npm run typecheck`/`test` (35/35) clean after the `package.json` edit. Grepped the whole repo for leftover "all rights reserved"/"private, personal project" phrasing to confirm nothing contradicts the new license; none found. Confirmed no em dashes crept into either new section, keeping the earlier cleanup pass intact.

**Deliberately not done:** the GitHub repo itself is still private, and I can't flip that myself (repo-visibility/access-control changes are outside what I'll do autonomously, full stop, regardless of instruction) - that one's for Will to flip in Settings whenever he's ready. The license text applies to the software itself and is correct either way.

## 2026-07-11 - Public-release polish: installers, README/author/donation setup, em-dash cleanup (direct request)

**Built:** the "make it look like a proper release for the public" ask, in four parts.

- **Author/branding**: `package.json`'s `author` field now reads `{ "name": "will.be", "url": "https://willbe.dev" }` (was a plain "Quant In A Box" string), and the electron-builder `copyright` field now reads "Copyright © 2026 will.be". Version bumped to `1.0.0` ahead of the first tagged release.
- **Donation setup**: added `.github/FUNDING.yml`, mirroring the pattern from the `gitsidian` public repo (GitHub Sponsors button + Buy Me a Coffee handle `calclab` + `willbe.dev` custom link, reusing the existing donation page rather than standing up a new one). README got matching "Support" and "Author" sections.
- **README overhaul**: added platform download badges (pointing at `releases/latest`, ready once the v1.0.0 tag ships installers), a Support section explaining the unsigned-build cost tradeoff, and an Author section crediting will.be / willbe.dev. License section clarified as private/all-rights-reserved (no LICENSE file exists; this isn't an open-source release).
- **Em-dash reduction**: reviewed and rephrased em-dash usage across README.md, BUILD_LOG.md, and the English UI strings (`en.json`, ~60 instances across Academy lesson content and short UI copy) into periods, commas, colons, and parentheses depending on what read most naturally. Left the 10 translated locale files untouched (out of scope: that's a translation-quality concern, not a style-tell one, and editing punctuation in languages I don't have native fluency in risks introducing an actual error).

**Verified:** `npm run typecheck`/`test` (35/35)/`build` all clean after the em-dash rewrite (values changed, not JSON structure). Re-ran the i18n key-parity script; still 236/236 keys matching across all 11 locales, confirming the rewrite didn't touch key names. Spot-checked the rewritten README and BUILD_LOG entries for readability after conversion.

**Real bug caught cutting the actual release:** tagging `v1.0.0` and pushing it to trigger the packaging matrix, all three platform jobs failed identically with `403 Forbidden: Resource not accessible by integration` when electron-builder tried to POST a new GitHub Release. Root cause: the workflow's auto-generated `GITHUB_TOKEN` defaults to read-only `contents` permission unless a job explicitly requests write access, which this workflow never did (it happened to work in earlier plans only because no tag had actually been pushed yet to exercise this path). Fixed by adding `permissions: contents: write` to the `package` job. Re-tagged (deleted and recreated `v1.0.0` on the fixed commit, confirmed no partial release existed first) and reran: all three jobs succeeded. Draft release `v1.0.0` now exists with `Quant-In-A-Box-Setup-1.0.0.exe`, `Quant-In-A-Box-1.0.0-arm64.dmg`, and `Quant-In-A-Box-1.0.0.AppImage` attached (plus their `.blockmap`/`.yml` auto-update metadata).

**Known limitation:** the macOS build only covers `arm64` (Apple Silicon); GitHub's `macos-latest` runner is arm64-native and `package.json`'s `mac` target doesn't request a `universal` or `x64` build, so Intel Mac users don't have a native installer yet. Not asked for; flagged here rather than silently shipped as "all platforms" when it's really "all platforms, Apple Silicon only" for macOS specifically.

**Deliberately not done without asking first:** the repo is still private and the `v1.0.0` release is still a draft (neither is visible to the public yet). Making the repo public and publishing the release are both one-way, externally-visible actions, so they're left for Will to confirm explicitly rather than done as part of this pass.

## 2026-07-11 - Fresh adversarial review of the layout feature: 18 confirmed bugs found and fixed

**Built:** nothing new; ran a second, more thorough adversarial review pass over the drag-and-drop dock/motion/risk-face feature (and everything built on top of it since, including the multi-portfolio work), this time across 5 dimensions instead of 3 (correctness, accessibility, i18n, performance, UX-polish), each with a 3-vote adversarial verify pass per finding before trusting it.

**18 confirmed, fixed bugs:**
1. **Correctness**: `PortfolioPane`'s AI-insights effect cleared `aiResult`/`aiError` on a position change but not `aiLoading`, and didn't guard against a slow AI response for an old portfolio composition landing after the user had already changed holdings. Fixed with a request-id ref, mirroring the `cancelled`-flag pattern the risk-stats effect already used.
2. **Accessibility**: a dock card's Move Up/Down button became `disabled` the instant it reached the boundary it was just used to reach, and since it held focus, the browser silently blurred it to `<body>`. Fixed with a `useLayoutEffect` that restores focus to a sibling action button in the same card.
3. **Accessibility**: the card-title collapse/expand toggle was a `<div role="button">` with no `tabIndex`, unreachable by keyboard despite advertising interactive ARIA semantics. Fixed with `tabIndex={0}` and a keydown handler.
4. **Accessibility**: `aria-expanded` was set on that same non-focusable title div instead of the actual focusable chevron button that performs the same toggle. Fixed by moving it to the button.
5. **Accessibility**: opening or closing a News article swapped the entire list/reader subtree with no focus management, dropping keyboard focus to `<body>` both ways. Fixed with refs that move focus to the back button on open and back to the originating headline button on close.
6. **Accessibility**: selecting a Portfolio symbol-search suggestion unmounted the results list (including the just-activated button), dropping focus to `<body>` instead of advancing to the quantity input. Fixed with a ref that focuses the quantity field after selection.
7. **i18n**: default portfolio names ("Portfolio 1", "Portfolio 2", …) were hardcoded English template strings, never routed through `t()`, unlike every other label in the feature. Fixed with a new `portfolio.defaultName` key translated into all 11 locales.
8. **i18n**: `IconArrowLeft` (the News "back" affordance) had no RTL mirroring, pointing the wrong direction in Arabic/Urdu. Fixed with a `dir === 'rtl'` check applying `scaleX(-1)`.
9. **i18n**: the risk-face colored stripe accent on stat tiles was pinned with a physical `left: 0` instead of a logical position, so it didn't flip sides under RTL. Fixed with `inset-inline-start`.
10. **i18n**: the Options Greeks table's "Vega" and "IV" column headers were hardcoded English literals, inconsistent with the adjacent translated "Strike" header. Fixed with new `dock.options.vega`/`dock.options.iv` keys across all 11 locales.
11. **Performance**: the whole `AppStateContext` value was one monolithic `useMemo`, so a dock-card drag (which mutates `dockOrder` on every step) re-rendered the entire app tree, including fully closed/off-screen panels. Fixed by splitting dock-layout state into its own `DockLayoutContext`/`useDockLayout()` hook, so unrelated consumers no longer re-render during a drag.
12. **Performance**: `DockCardOverlay` unconditionally called the risk/options/news data hooks regardless of which card (if any) was expanded, duplicating real network fetches for data never displayed. Fixed by splitting each into its own conditionally-mounted body component.
13. **Performance**: `AcademyPanel`'s `categoryLabel` and `activeLesson` were recomputed unmemoized on every render, even while the panel was closed. Fixed with `useMemo`, matching the pattern already used for `grouped`.
14. **UX-polish**: the risk-face and info-icon tooltips inside a stat tile were almost entirely clipped by the tile's own `overflow: hidden`. Fixed with a `:has(.tooltip-bubble)` rule that switches to `overflow: visible` only while a tooltip is actually rendered.
15. **UX-polish**: dock card head tooltips (drag handle, move up/down, expand) suffered the same clipping problem. Fixed by giving them `side="right"`, matching an existing fix already applied to the ticker settings button.
16. **UX-polish**: the close-button tooltip in every `OverlayPanel`-based dialog was clipped by the panel's own `overflow: hidden`. Fixed by removing that overflow from the panel and adding `overflow-x: hidden` to the scrollable body instead, so the rounded corners and scroll clipping are both preserved.
17. **UX-polish**: `PortfolioWorkspace` sized itself with `96vw`/`92vh` instead of the `min(x, 100%)` pattern every sibling overlay used, so it ignored the scrim's 40px padding and sat with an inconsistent margin. Fixed to match.
18. **UX-polish**: `DockCardOverlay` never passed the `layoutId` prop `OverlayPanel` was built to support, so the "card grows into this overlay" shared-element morph never actually rendered; it just faded in like every other dialog. Fixed by wiring up the matching `layoutId`.

**Verified:** `npm run typecheck`/`test` (35/35)/`build` all clean after all 18 fixes landed together, including the `AppStateContext` split (the highest-risk change, since it touches every consumer's import). Re-ran the i18n key-parity script: 236/236 keys matching across all 11 locales (3 new keys added by the fix agents). Restarted the dev server to rule out stale Vite HMR state from the many concurrent edits before browser-verifying; confirmed a clean load with no console errors. Directly re-verified 11 of the 18 fixes live in the browser: Move Up/Down focus restoration (moved a card, confirmed focus landed on the sibling button, not `<body>`), the title-toggle's new `tabIndex`, `aria-expanded` correctly on the chevron button, RTL icon mirroring (`matrix(-1, 0, 0, 1, 0, 0)` under `dir="rtl"`, `none` under LTR), the RTL stripe (`inset-inline-start: 0` resolving to the tile's right edge under RTL), the stat-tile tooltip's `:has()` overflow fix (bubble renders, ancestor stays `overflow: visible` only while it's present), the overlay-panel close-button tooltip fix (`overflow: visible` on the panel), `PortfolioWorkspace`'s corrected sizing (1200px, matching `min(1400px, 100%)` against the padded scrim), the `DockCardOverlay` `layoutId` prop, and News reader focus management in both directions (open moves focus to the back button, close restores it to the originating headline). The remaining fixes (AI-insights staleness guard, the `AppStateContext` split's render-count improvement, `DockCardOverlay`'s duplicate-fetch elimination) were verified via passing typecheck plus direct code review of the fix, not a live interaction test.

## 2026-07-11 - Persistent multi-portfolio support + thousands-strong searchable asset universe (direct request)

**Built:** the two-part ask: "persistent portfolios that you can name... and open concurrently" plus "far more default assets... probably thousands."

- **Multi-portfolio data model**: `Portfolio { id, name, positions }` replaces the single flat position array. New `AppStateContext` API: `createPortfolio(name?)` (auto-names "Portfolio N", smallest unused N, if omitted), `renamePortfolio`/`deletePortfolio`, `addPosition`/`removePosition` (all re-parameterized by `portfolioId`), `openPortfolio`/`closePortfolioInstance`/`closeAllPortfolios`, `openPortfolioIds` (ephemeral), `allPortfolioSymbols` (deduped union across every portfolio, feeding the ticker tape, news relevance, and both Rail/Topbar badge counts). Persisted to a new `qiab:portfolios:v2` key; the old single-array `qiab:portfolio:v1` is read once as a migration fallback (wrapped into a "Portfolio 1") and left untouched afterward.
- **Concurrent viewing**: one shared `PortfolioWorkspace` (`OverlayPanel`) renders a horizontally-scrollable row of `PortfolioPane`s, one per open portfolio, each with fully independent position/analytics/AI-insights state (plain component-local `useState`, no extra plumbing needed). Reuses the already-hardened `OverlayPanel` Escape/focus-trap logic as-is; no changes needed there. A dashed "+ New portfolio" pane always sits at the end of the row so you can add another regardless of how many are already open.
- **Entry points**: `PortfolioPicker` (Rail + Topbar, via a shared render-prop trigger). 0 portfolios creates-and-opens one, exactly 1 opens it directly, 2+ shows a popover checklist (open/close per portfolio + "+ New portfolio"). Rename/delete live in Customize's new "Portfolios" section: inline click-to-rename with duplicate-name rejection, delete behind a "click again to confirm" (no separate modal).
- **Asset universe expansion**: real, sourced data, not hand-typed. Roughly 11,123 stock/ETF symbols from NASDAQ Trader's public symbol directory (`nasdaqlisted.txt`/`otherlisted.txt`), roughly 490 real cryptocurrencies from CoinGecko's public `/coins/markets` endpoint, plus hand-curated expansions for bonds/FX/real-estate (24/20/19 entries; "thousands" genuinely doesn't apply to these three classes, called out here rather than inflated). Every entry gets a deterministic seeded price/changePct via the existing `hashSymbol`/`seededRandom` machinery, still simulated data by design, same as every other asset in the app, just at roughly 800x the previous scale (14 to roughly 11,653).
- 8 new i18n keys (`portfolio.pickerHeading`/`newPortfolioBtn`, 6 `customize.portfolio*` keys) translated into all 10 languages; key-parity script confirms 233/233 across all 11 locales.

**Data-quality bugs caught and fixed before trusting the generated universe:**
1. A leftover `" - "` in cleaned stock names (name-cleaning regex removed "Common Stock" but not the trailing separator). Fixed with an added trim step.
2. **Real symbol collision**: BTC and ETH exist both as cryptocurrencies and as real NYSE-listed ETF tickers (Grayscale's Bitcoin/Ethereum trust products trade under those exact symbols), which would have silently shadowed the intended crypto assets in `find()`-by-symbol lookups. Fixed with a priority-ordered `usedSymbols` set (hand-curated first, real crypto second, generated stocks last, dropping roughly 120-150 colliding stock entries).
3. Plural "Units"/"Warrants"/"Rights" SPAC instruments slipped past a singular-only bad-name filter regex. Fixed to match both forms.
4. 7 very-low-value memecoins rounded to exactly `price: 0` at the original precision, which would break candle-generation math. Fixed with higher precision plus an explicit `price <= 0` skip.

**Bug caught and fixed during live verification (not in the data, in search ranking):** with the stock universe now roughly 11k entries sorted alphabetically ahead of crypto/bonds/FX/RE in `ALL_ASSETS`, the existing naive substring-filter-then-`.slice(0,6)` search meant a query for a real, popular coin could be entirely crowded out by unrelated long-tail stock names that happen to contain the same substring. Confirmed live: searching "SOL" returned six "...Solutions Inc." stock matches and never surfaced the actual Solana coin at all. Fixed with a shared ranked-search helper (`lib/assetSearch.ts`) used by all three search call sites (Topbar, Customize watchlist-add, Portfolio add-position): exact symbol match ranks first, then symbol-prefix, then name-prefix, then symbol-substring, then name-substring. Re-verified live afterward: "SOL" now surfaces Solana first, "BTC" surfaces Bitcoin first, with prefix-matching ETFs correctly following.

**Verified:** typecheck/test(35/35)/build clean throughout. Live browser verification: seeded the legacy `qiab:portfolio:v1` key and confirmed it migrates correctly into a named "Portfolio 1" on reload; created a 2nd and 3rd portfolio via the new workspace affordance and confirmed unique auto-naming (Portfolio 1/2/3); opened all three concurrently and confirmed independent per-pane position state (adding AAPL to Portfolio 2 left Portfolio 1's NVDA/BTC and Portfolio 3's empty state untouched); confirmed a pane's own close button closes only that pane while Escape closes the whole workspace; confirmed Rail and Topbar badges both correctly show the deduped union count (3) across all portfolios; confirmed Customize's rename correctly rejects a duplicate name with an inline error and accepts a valid custom name ("Growth Basket"), and that delete requires the confirm-click before removing a portfolio; confirmed the expanded asset universe is actually searchable live in the UI, not just verified via standalone scripts (GOOGL, AAPL, multiple leveraged-AAPL ETFs, and, after the ranking fix, SOL/BTC all resolve correctly).

**Known limitation:** no way (yet) to reorder or pin which portfolio opens by default beyond "most recently used." Not asked for, not built.

## 2026-07-11 - Fluid layout: drag-and-drop dock cards, motion system, risk-face indicators (direct request)

**Built:** the three-part ask: "fluid movement profile," draggable/poppable dashboard layout, and good/neutral/bad risk-stat faces.

- **Motion system**: added the `motion` package (framer-motion's successor). New `--duration-*`/`--ease-*` tokens in `theme.css`, shared spring/fade presets in `lib/motion.ts`, `<MotionConfig reducedMotion="user">` wrapping the whole app so every animation collapses under the OS's reduced-motion setting exactly like the existing CSS kill-switch.
- **Drag-to-reorder + show/hide dock cards**: Risk/Options/News cards are now a `Reorder.Group`/`Reorder.Item` list (real pointer-drag, verified with an actual OS-level drag gesture, not just simulated events) backed by new persisted `dockOrder`/`dockHidden` state (`qiab:dockLayout:v1`). Keyboard users get explicit Move up/down buttons on each card header (`Reorder.Item` has no built-in keyboard support) computed against the *visible* order so hidden cards don't break adjacency. Show/hide + "Reset layout" live in a new Customize panel section.
- **Pop-open card overlay**: each card gets an Expand button opening a larger `OverlayPanel` view of the same content (shared body components extracted so the docked and expanded views never duplicate JSX).
- **Shared `OverlayPanel`**: extracted from three near-identical duplicated implementations (Portfolio/Academy/Customize), adding a real open/close animation in the process. All three panels, plus the new card overlay, now share one scrim/dialog/Escape/focus implementation instead of three copies.
- **Risk-face indicators**: Sharpe/Sortino/Volatility/VaR/MaxDrawdown/Beta each get a green/orange/red face (smile/neutral/frown) computed from the metric's actual value against a documented threshold table (`lib/riskAssessment.ts`, unit tested), replacing a `tone` prop that was previously hardcoded per metric type rather than computed. Beta's face is explicitly framed as "distance from market-typical," not a performance judgment, with a tooltip caveat saying so (confirmed with Will before building since beta has no inherent good/bad direction). Consolidated two duplicated stat-tile components (`PStat` in `PortfolioPanel`, `StatTile` in `RiskCard`) into one shared `RiskStatTile`.
- Translated the 29 new strings (`card.*`, `riskFace.*`, new Customize section) into all 10 languages; key-parity script confirms 225/225 keys match across all 11 locales.

**Adversarial review + fixes (this is the part worth reading closely):** ran a 3-dimension parallel review (correctness/accessibility/i18n) with an independent verify pass on every finding before trusting it. i18n came back clean. Correctness and accessibility did not: 5 real, confirmed bugs, all fixed before calling this done:
1. Reopening the News card overlay after reading an article showed the stale reader view instead of the fresh list (`DockCardOverlay`'s `openItem` state never reset on close; it's a permanently-mounted component, only its visual presentation toggles).
2. **No focus trap**: Tab could escape an open dialog into fully-interactive background content (e.g. Portfolio's add-position inputs, sitting behind Academy's higher z-index but still focusable).
3. **Escape closed every open overlay at once**, not just the topmost: each `OverlayPanel` instance independently listened on `window` with no coordination, so Academy-opened-from-inside-Portfolio meant one Escape press discarded both.
4. **No focus restoration**: closing a dialog dropped focus to `<body>` instead of back to the button that opened it.
5. The risk-face tooltip was mouse-only: its trigger `<span>` had no `tabIndex`, so keyboard users could never reach the explanation text.

Fixed all 5 in `OverlayPanel.tsx` (a module-level open-instance stack so only the topmost responds to Escape, a real Tab-cycling focus trap queried against the dialog's own focusable elements, capture-and-restore of the previously-focused element), `DockCardOverlay.tsx` (reset `openItem` whenever `expandedCard` isn't `'news'`), and `RiskStatTile.tsx` (`tabIndex={0}` on the face trigger). All 5 individually re-verified live in the browser afterward, not just re-read the code, including a real Tab keypress confirming the focus trap wraps correctly and a real Escape keypress confirming only the topmost of two simultaneously-open overlays closes.

**Verified:** typecheck/test(35/35)/build clean throughout. Live browser verification: real pointer-drag reorder (persisted across reload), keyboard move-up/down, hide/show + reset via Customize, pop-open overlay open/close, light theme, Arabic RTL (including the new strings and `dir="rtl"`), and, after the fix pass, all 5 confirmed bugs individually re-tested and confirmed fixed.

## 2026-07-11 - AI portfolio insights, powered by your local Claude Code session (direct request)

**Built:** the "Get AI insights" feature from the original plan (Stage B), redesigned per Will's request to avoid requiring a fresh Anthropic API key: `("the need for API key makes it pretty annoying. Can we somehow link it to claude code on the computer?")`.

- **`src/main/aiInsights.ts`** (new): `getPortfolioInsights()` tries, in order: (1) the user's local Claude Code CLI, invoked non-interactively (`-p`, `--tools ""` to fully disable filesystem/Bash access, `--model sonnet`, `--json-schema` for structured output, `--max-budget-usd 0.25` as a cost backstop), (2) a direct Anthropic Messages API call using `ANTHROPIC_API_KEY` if the CLI isn't available or fails. Both paths return the same shape so the UI doesn't care which one answered.
- **`src/main/index.ts`**: two new IPC handlers, `ai:checkAvailability` (reports whether the CLI and/or an API key are usable, so the UI can show the right state before the user even clicks anything) and `ai:getPortfolioInsights`.
- **`src/preload/index.ts` / `index.d.ts`**: typed `window.api.checkAiAvailability()` / `window.api.getPortfolioInsights(request)` bridge.
- **`PortfolioPanel.tsx`**: new "AI portfolio insights" section below the analytics grid. Connect-prompt empty state (neither backend available) leads to a "Get AI insights" button, then loading, then commentary plus a **permanent, always-rendered disclaimer banner** ("Not financial advice", full explanatory body, amber warning styling with an alert-triangle icon) plus source attribution (Claude Code vs. API key) plus Regenerate, or an error state with Try again. The disclaimer is the app's own static translated copy, not something the model is trusted to remember to include.
- 12 new i18n keys under `portfolio.ai.*`, translated into all 10 other languages (matching each locale's existing portfolio-section terminology), verified with the same key-parity script used for the original 10-language rollout: 196/196 keys match with 0 missing/extra/empty across all 11 locales.
- `.env.example` documents `ANTHROPIC_API_KEY=` (explicitly **not** `VITE_`-prefixed, confirmed via `grep`-ing the built renderer bundle that the actual key value never appears there, only the UI copy that mentions the env var name as user-facing instructions).

**Real bugs caught during verification (all fixed, not just noted):**
1. **Windows spawn failure.** `child_process.spawn('claude', ...)` failed with `ENOENT` on this machine because npm installs `claude` as a `.cmd` shim on Windows, and Node refuses to spawn `.cmd` files directly without a shell (a deliberate Node security guard, not a bug in our code). The naive fix, `shell: true`, is explicitly deprecated by Node for this exact call shape because it stops escaping arguments and only concatenates them. Fixed by switching to the `cross-spawn` package (industry-standard fix for this exact Windows quirk, used internally by npm/yarn), verified with a direct end-to-end round trip on this machine using the real flags (`--tools ""`, `--model sonnet`, `--json-schema`, `--max-budget-usd`) before trusting it.
2. **3-second dead time per call.** Left as default `stdio`, the CLI printed a stderr warning and waited ~3s to see if piped stdin was coming before proceeding. Fixed with `stdio: ['ignore', 'pipe', 'pipe']` so stdin is closed immediately; confirmed the warning disappears and the wait is gone.
3. **Panel-crashing bug.** The availability-check effect called `window.api.checkAiAvailability()` with no guard; if `window.api` were ever unavailable for any reason, this threw and crashed the entire `<PortfolioPanel>` (caught live: reproduced by loading the renderer outside real Electron, where `window.api` doesn't exist). This directly violated the standing "never crash, never silently fail" requirement, so it's treated as a real bug, not a contrived edge case. Fixed with a `Promise.resolve(...).catch(...)` guard that degrades to the "connect a backend" empty state instead.
4. **Contradictory simultaneous UI states.** The four result/loading/error/button blocks were independent `&&` conditions instead of mutually exclusive branches; regenerating after a failure left the *stale success result* and the *new error* both rendered at once. Fixed by clearing both `aiResult`/`aiError` at the start of every fetch and requiring the button to check `!aiError` too.

**Verified:** `npm run typecheck`/`test` (27/27)/`build` all clean. Confirmed live in the browser (dev server against the raw Vite renderer, since `window.api` needs real Electron; the connect/button/loading/result/error states were exercised by mocking `window.api` after confirming the real backend logic separately): connect-empty-state renders with no crash when no backend is present, button leads to mocked success rendering commentary + disclaimer + correct source line for both `claude-code` and `api-key`, mocked failure renders a clean error with no leftover stale state, and French translation confirmed rendering correctly for the new section. The actual Claude Code CLI round trip (not mocked) was verified directly via Node scripts using `cross-spawn` with the exact production flags, run natively on this Windows machine: full JSON response with `structured_output.commentary`, `permission_denials: []` (confirming `--tools ""` really blocks tool access), and real cost/usage data.

**Not yet verified:** the Anthropic-API-key fallback path (`runAnthropicApi`) hasn't been exercised against a live key; needs Will's own key in `.env` to confirm end-to-end (the Claude Code CLI path, which will be the common case since Will already has it installed and signed in, is fully verified).

## 2026-07-11 - 10 languages registered: zh, hi, es, fr, ar, bn, pt, ru, ur, th (direct request)

**Built:** All 10 previously-translated locale files (`src/renderer/src/i18n/locales/{zh,hi,es,fr,ar,bn,pt,ru,ur,th}.json`) are now wired into `i18n/index.ts`: imported, added to the i18next `resources` object, and registered in `SUPPORTED_LANGUAGES` with each language's native-script label (中文, हिन्दी, Español, Français, العربية, বাংলা, Português, Русский, اردو, ไทย) and correct `dir`. All 10 are now selectable in the Topbar language dropdown (which already mapped over `SUPPORTED_LANGUAGES`, so no dropdown changes were needed) alongside English.

**Translation quality pipeline (per language, applied before this cycle and re-checked here):** full translation of all 183 keys, then a structural key-parity check against `en.json` (every locale confirmed to have exactly the same 183 leaf keys, no missing/extra), then a native-fluency spot-check/fix pass. Re-verified the parity step directly in this cycle with a script that deep-diffs every locale's key tree against `en.json`: all 10 came back with 0 missing / 0 extra keys. Also spot-checked all 3 interpolated strings (`dock.news.minutesAgo`, `dock.news.hoursAgo`, `academy.infoTooltip`) across every locale: the `{{count}}`/`{{title}}` placeholders survived translation intact in all 10, which is the part most likely to get mangled by a translation pass.

**RTL scope (important, read before assuming Arabic/Urdu are "done"):** `ar` and `ur` have correct **text** direction wired up: `dir: 'rtl'` in `SUPPORTED_LANGUAGES`, which drives `document.dir` via `AppStateContext`, so RTL script renders and flows correctly. What is explicitly **not** in scope this pass: the dashboard's **panel layout** (rail/workspace/dock positions, chart orientation) is not mirrored for RTL; it stays in its LTR arrangement even when an RTL language is active. This is a deliberate, known limitation, not an oversight; full RTL layout mirroring is a larger structural change (flipping flex/grid direction across Topbar/Rail/Workspace/Dock, checking every hardcoded left/right in chart tooling) that wasn't part of this task.

**Known limitation, these are AI-generated translations:** every one of the 10 locale files was produced by the translate → key-parity → fluency-spot-check pipeline above, not by a native speaker. They should be treated as a solid first pass, not final copy; a real native-speaker review is recommended before trusting them fully for production users, especially the financial terminology in the Academy lessons (Sharpe/Sortino/VaR/Greeks/etc. have real, sometimes non-obvious, established translations in each market's financial vocabulary that a fluency spot-check by a non-native pipeline can plausibly miss).

**Verified:** `npm run typecheck` clean (both `tsconfig.web.json` and `tsconfig.node.json` projects), `npm run test` 27/27 passing, `npm run build` clean (main/preload/renderer all built, renderer bundle 870.90 kB / 36.60 kB CSS). All three re-run after the `index.ts` edit to confirm the new imports resolve correctly; no malformed JSON or import path issues found in any of the 10 locale files.

## 2026-07-11 - Styling review: icons, tooltips, richer chart hover, news relevance, asset-class education (direct request, mid-loop)

**Built (four pieces, three commits):**
1. **Icon system + i18n tooltips**: new `Icons.tsx` (stroke-based SVG set) replaces every emoji/Unicode glyph app-wide. New `Tooltip` component wraps icon-only buttons with translated text, verified via real hover simulation (not synthetic events; see note below) and via ARIA labels.
2. **Chart hover enrichment**: the crosshair readout now shows date, volume, change vs. the previous bar, and active indicator values (MA20/MA50/Bollinger), via a cached indicator-array lookup keyed to the hovered bar's index. Also fixed mock candle volume, which was a raw 0-1 fraction (fine when only used for bar-height, wrong once displayed as a number); rescaled to a realistic 200K-8M range.
3. **News relevance**: Market News can focus on whatever's selected, your watchlist, or your portfolio, chosen in Customize and persisted. Expanded the mock news pool 5→11 templates for real coverage differences between sources, with a fallback to the full feed when a filter matches nothing.
4. **Teaching Zone asset-class lessons**: 5 new lessons (Stocks & ETFs, Crypto, Bonds, FX & Commodities, Real Estate/REITs) in a new "Asset Classes" category, placed first in the Academy nav. Each has a genuinely relevant formula per class and watchOutFor guidance aimed directly at the panic-buy/panic-sell instinct.

**Testing note worth recording:** this session's screenshot tool was broken (`computer{action:"screenshot"}` timed out repeatedly), and synthetic `MouseEvent`/`PointerEvent` dispatch doesn't trigger either React's mouseenter/mouseleave (needs real `mouseover` with correct `relatedTarget`) or lightweight-charts' canvas interaction (the library's own event binding doesn't respond to script-dispatched events). Worked around this with: DOM/ARIA inspection instead of visual checks, `computer{action:"hover", ref:...}` (real OS-level mouse movement) for the one interaction that genuinely needed it, and relying on the actual Electron window being open for Will to verify anything requiring true visual/interactive confirmation.

**Verified:** typecheck/test(27/27)/build clean across all three commits. Extensive DOM/ARIA-level verification in the browser for each piece (documented per-commit); CI green on all pushes.

## 2026-07-11 - Ticker customization + sidebar redesign (direct request, mid-loop)

**Built:**
- Ticker tape settings gear: choose watchlist/portfolio/all as its source, persisted, with a proper empty-state message when the source has nothing.
- Sidebar: removed the 6 duplicate asset-class buttons (topbar chips already do this; Will correctly flagged it as a pointless double-up). New "Make it yours" entry opens a Customize panel with real watchlist management: add via search, remove, reset to defaults.

**Real bug caught during testing:** the ticker's source popover originally closed via `onMouseLeave`. Testing revealed this is fragile: a user's mouse moving from the gear button toward a menu item can exit the combined bounding box mid-path (there's a gap between the button and the popover, which sits `bottom: 40px` above it) and close the menu before they can click anything. Replaced with the standard click-outside-to-close pattern (document `mousedown` listener + refs). Also learned during testing that synthetic same-tick `.click()` + immediate DOM query in test scripts doesn't reliably reflect React's state flush; real interactions (separate ticks) work correctly. This was a test-harness artifact, not an app bug, confirmed by re-checking in separate calls each time.

**Verified:** typecheck/test(27/27)/build clean. Full interactive flow confirmed in the browser: ticker source switching with correct empty states, watchlist add (via autocomplete)/remove/reset all working.

## 2026-07-11 - Real logo wired up (direct request, mid-loop)

**Built:** Will dropped three logo assets into `assets/` (logo-full.png: combined lockup, transparent bg; logo-just.png: icon-only mark, black bg; logo-title.png: wordmark only, black bg) and asked for them to be used throughout the app as sensible, with logo-just as the app icon.
- Topbar brand mark now shows the real icon (candlestick breakout from a wireframe box) instead of the placeholder checkmark SVG.
- logo-just.png is now the actual packaged app icon (Windows/Mac/Linux via electron-builder) *and* shows correctly in the dev-mode taskbar (not just packaged builds); required adding a `resources/` folder packaged alongside `out/` and pointing both electron-builder's icon config and the BrowserWindow's `icon` option at it.

**Scope call:** didn't force logo-full.png or logo-title.png into the UI; neither has a natural fit in the current dense dashboard without redesigning something (both also have baked-in black backgrounds or an image aspect that doesn't suit the slim topbar), so they're kept as source assets for later (e.g. a splash/about screen) rather than shoehorned in.

**Known limitation (resolved 2026-07-11):** at the time of writing this app icon had no image-processing tooling (ImageMagick/PIL/sharp) available to pre-process it into a full multi-size set. A proper `resources/icon.ico`/`icon.png` pair now exists and is wired into `package.json`'s `build` config; see the public-release-polish entry above.

**Verified:** typecheck/test(27/27)/build clean. Confirmed in the browser that the topbar image loads with correct dimensions and the rounded badge CSS applies. Ran a real `build:unpacked` packaging pass; the "default Electron icon is used" warning is gone, confirming a real `icon.ico` now generates from our source.

## 2026-07-11 - i18n foundation: every string extracted, English verified

**Built:** Installed `react-i18next` + `i18next`. Every hardcoded string in the app (topbar, rail, workspace toolbar, all three dock cards, the full Academy, including all 12 lessons' title/summary/formula/howToUse/watchOutFor text, and the Portfolio panel) now routes through `t()` against `src/renderer/src/i18n/locales/en.json`. `lessons.ts` was refactored to hold only metadata (id + category); display text lives entirely in the locale file so each language translates independently. Added a language switcher in the Topbar (persists like the theme toggle) and RTL-direction plumbing in `AppStateContext` (a `SUPPORTED_LANGUAGES` metadata list drives `document.dir`/`lang` attributes), ready for Arabic/Urdu.

**Why this order:** this was flagged as the highest-risk step in the plan, a refactor touching nearly every component with real potential to silently break something (a missed string, a broken prop, a t() key typo showing raw dot-paths on screen), so it had to be verified thoroughly before spending effort on 10 rounds of translation on top of a possibly-shaky foundation.

**Verified:** typecheck/test (27/27)/build all clean. In the browser (via the same dev server now also open as a real Electron window for Will to test directly): confirmed the workspace, indicator chips, watchlist tooltips, and the Academy panel's full lesson content (spot-checked "Moving Average") all render byte-identical to before the refactor, sourced entirely from the new key structure. Confirmed the Portfolio panel's dialog and all its placeholders render correctly too. No console errors.

**Housekeeping note:** `logo-full.png` appeared in the project root (not created by me, presumably an early logo draft Will dropped in). Left untracked/uncommitted rather than silently swept into this commit; flagged for Will to confirm what to do with it.

**Next up:** translating into the 10 target languages, starting with the next loop cycle.

## 2026-07-10 - Portfolio tracker v1

**Built:** A real "Your holdings" panel (topbar/rail button, opens like the Teaching Zone) where you add positions by symbol + quantity + average cost basis. Adding to an existing symbol blends into a new weighted average cost rather than duplicating the row. Shows per-position current price, market value, unrealized P&L $/%, and % of portfolio weight, plus portfolio-wide totals. Persisted to `localStorage` (`qiab:portfolio:v1`), same pattern as the watchlist.

The bigger piece: **Portfolio Analytics** now computes real weighted stats (Sharpe, Sortino, volatility, VaR, max drawdown, beta) across your *actual* holdings, not just whatever symbol happens to be selected in the main chart. `lib/portfolioMath.ts` blends each holding's daily-return series by its market-value weight into one portfolio-level return series, then feeds that into the same `lib/quant.ts` functions the single-symbol Risk card already uses. New unit tests (`portfolioMath.test.ts`, 4 tests) cover the blending and compounding math.

**Scope decisions made without asking** (flagged here in case any should be revisited): single portfolio, not multiple; current positions only, no transaction/lot history. Matches the most common "simple portfolio tracker" pattern and was explicit enough in what Will asked for ("track how it's going, apply the analytics to it") to build directly rather than re-asking.

**Verified:** typecheck/test (27/27)/build all clean. Functional check in the browser: added NVDA (10 @ $120), P&L and weight (100%) computed correctly, and portfolio analytics with one holding matched that symbol's own standalone Risk-card stats exactly (a strong correctness cross-check). Added BTC (0.05 @ $58,000): totals, weights (30.8%/69.2%), and analytics all updated correctly. Removed NVDA: analytics correctly collapsed back to BTC's own standalone stats. Reloaded the page and confirmed both the position list and the topbar's "Portfolio (n)" count persist.

**Bug caught and fixed:** opening the Teaching Zone from an info-icon *inside* the Portfolio panel opened Academy behind it (both modals shared `z-index: 100`, and Portfolio mounts later in the DOM so it won the tie). Fixed by giving Academy a higher `z-index`; it's a contextual reference lookup that should always stack above whatever invoked it.

## 2026-07-10 - GitHub repo + real cross-platform CI, with two real bugs caught and fixed

**Built:**
- Private repo created and pushed: [github.com/WillBe89/quant-in-a-box](https://github.com/WillBe89/quant-in-a-box).
- `.github/workflows/build.yml`: typecheck+test on every push/PR; on a `v*` tag, Windows/macOS/Linux runners each build their native installer and publish all three to a draft GitHub Release; manual runs (`workflow_dispatch`) build and upload as workflow artifacts instead, for testing without cutting a release.

**Bugs caught by actually running CI, not just reading the config:**
1. `npm ci` failed on a clean checkout with "Missing: esbuild@0.28.1 from lock file." Root cause: `vitest@4.1.10`'s `peerDependencies` require `vite ^6/^7/^8`, incompatible with this project's `vite@^5.4` (pinned for `electron-vite`). npm was silently auto-installing that peer as a private nested `vite`+`esbuild@0.28.1` inside `vitest/node_modules`, a resolution that varied run-to-run locally and produced a lockfile `npm ci` correctly rejected as inconsistent on a fresh Linux checkout. First attempted fix (approving the extra esbuild script) treated the symptom, not the cause, and CI failed again identically. Real fix: downgraded to `vitest@3.2.7`, which depends directly on `vite: "^5.0.0 || ^6.0.0 || ^7.0.0-0"`, no nested copy needed at all. Verified with a genuine from-scratch `npm ci` locally (not just `npm install`) before pushing again.
2. Electron-builder's `nsis` config rejected `allowToChangeInstallDirectory`; not a real property, correct name is `allowToChangeInstallationDirectory`. Caught immediately by electron-builder's own schema validation on the first packaging attempt.

**Verified:** manually triggered the full workflow (`workflow_dispatch`) after the fixes; typecheck/test job green, and all three packaging jobs (windows-latest, macos-latest, ubuntu-latest) succeeded, producing real installer artifacts (~75-110MB each, sane sizes for an Electron app). Confirms a `v*` tag push will actually produce a working `.exe`, `.dmg`, and `.AppImage` in one GitHub Release.

## 2026-07-10 - Loop stopped: needs Will's input on the portfolio tracker

Three cycles completed autonomously (watchlist persistence, quant.ts test suite, packaging config), each typechecked, tested, and built clean, with real bugs caught and fixed by the verification step each time (a test-fixture bug in cycle 2, a wrong electron-builder property name in cycle 3).

The only remaining item from the candidate list is the portfolio/holdings tracker, and it's a genuine product-scope question, not something with an obvious default:
- Single portfolio, or multiple named portfolios?
- Current positions only (symbol, quantity, cost basis), or full transaction/lot history (so realized vs. unrealized P&L can be split out)?
- Should the existing "Portfolio Risk" dock card (today: stats for whichever symbol is selected) become an actual *weighted portfolio* calculation once real holdings exist, meaning its meaning changes for every user, not just an addition?
- Cash balance / uninvested cash tracked at all?

Stopping here rather than guessing, per the loop's stop condition. Everything else scoped in this conversation is built, tested, and verified working.

## 2026-07-10 - Packaging config (electron-builder)

**Built:** A real `build` config in `package.json` for electron-builder: `appId`, `productName`, output directory (`release/`), Windows NSIS installer target (with a changeable install directory and desktop shortcut), macOS DMG target (Finance category), Linux AppImage target. Added a `build:unpacked` script (`electron-builder --dir`) for fast local packaging verification without generating a full installer every time.

**Why:** the app had no path to becoming an actual distributable `.exe`/`.dmg`/`.AppImage` yet; this was pure config/plumbing needed before anyone outside a dev environment could run it.

**Verified:** first attempt failed fast and loud: `npm run build:unpacked` caught a real error, `nsis.allowToChangeInstallDirectory` isn't a valid electron-builder property (correct name is `allowToChangeInstallationDirectory`); electron-builder's own schema validation pointed at the exact typo. Fixed the property name, re-ran, and it produced a real working executable at `release\win-unpacked\Quant In A Box.exe`. No custom app icon exists yet, so electron-builder falls back to the default Electron icon; that's a design-asset gap (someone needs to draw/commission an actual icon), not a code blocker, so it's left as a known follow-up rather than something to guess at.

## 2026-07-10 - Unit tests for the quant math library

**Built:** A real test suite (`vitest`) covering every function in `lib/quant.ts`: SMA, EMA, stdev, daily returns, annualized volatility, Sharpe, Sortino, max drawdown, historical VaR, beta, Bollinger Bands, RSI, MACD, and Black-Scholes pricing/Greeks. 23 tests, all passing. Added `npm run test` script and `vitest.config.ts` (mirrors the `@renderer` path alias from `electron.vite.config.ts`).

**Why:** the app's whole pitch is "real institutional-grade math," so the math library is the highest-stakes code in the codebase; it deserved verification beyond "it compiles and looks right on screen." Black-Scholes is checked against the classic textbook ATM example (S=K=100, T=1, r=5%, σ=20% → price ≈ 10.45, delta ≈ 0.637) and against put-call parity; Sharpe/Sortino/VaR/beta are checked against hand-computable edge cases (identical-to-market beta = 1, zero-volatility Sharpe = 0, worst-percentile VaR, etc.).

**Verified:** `npm run test` → 23/23 passing. Caught one real issue along the way, not in the library, but in my own first draft of a Sortino test, which picked two identical downside returns, so downside deviation was correctly 0 and Sortino correctly short-circuited to 0 by the divide-by-zero guard; fixed the test's fixture data rather than the function. `npm run typecheck` and `npm run build` both still clean. No UI changed this cycle, so no browser check was needed.

## 2026-07-10 - Watchlist persistence

**Built:** Real watchlist add/remove, persisted across restarts.
- `AppStateContext` now loads/saves the watchlist to `localStorage` (`qiab:watchlist:v1`), defaulting to one symbol per asset class (NVDA, BTC, US10Y, EURUSD, VNQ) on first run.
- The star button in the symbol header (`Workspace.tsx`) toggles the current symbol in/out of the watchlist (filled vs outline star).
- The rail's pinned-symbol list (`Rail.tsx`) now reflects the actual watchlist instead of always showing every mock symbol, with an empty-state hint when nothing is pinned.

**Why:** the star button existed visually in v1 but did nothing; the rail always showed every symbol regardless of what the user cared about, which isn't a real "watchlist."

**Verified:** `npm run typecheck` and `npm run build` both clean. Functional check via the browser preview: toggled a symbol off/on, confirmed `localStorage` contents update, reloaded the page and confirmed the watchlist survives reload. Caught and fixed nothing further needed; the only console errors seen were stale HMR churn from earlier edits, not a live bug (confirmed by re-checking DOM state and a fresh reload).
